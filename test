<!DOCTYPE html>
<html lang="ko" class="h-full">
<head>
<script>
  window.__firebase_config = JSON.stringify({
    apiKey: "AIzaSyD8mdIPeNvukYQnhptkYzVvqjsET4DU4xE",
    authDomain: "bookcraft-studio-b2e20.firebaseapp.com",
    projectId: "bookcraft-studio-b2e20",
    storageBucket: "bookcraft-studio-b2e20.firebasestorage.app",
    messagingSenderId: "586232264727",
    appId: "1:586232264727:web:55573ebcd8be4df6b90452"
  });
</script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BookCraft Studio - 소설 구상 & 조판 워크스페이스</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        brand: {
                            50: '#f0f7ff',
                            100: '#e0effe',
                            500: '#3b82f6',
                            600: '#2563eb',
                            700: '#1d4ed8',
                            800: '#1e40af',
                            900: '#1e3a8a',
                        },
                        paper: {
                            white: '#ffffff',
                            cream: '#fdfbf7',
                            vintage: '#f5efe0',
                            dark: '#1e1e2e'
                        }
                    },
                    fontFamily: {
                        batang: ['Gowun Batang', 'Nanum Myeongjo', 'Noto Serif KR', 'serif'],
                        myeongjo: ['Nanum Myeongjo', 'serif'],
                        gothic: ['Noto Sans KR', 'sans-serif'],
                        mono: ['JetBrains Mono', 'monospace']
                    }
                }
            }
        }
    </script>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Gowun+Batang:wght@400;700&family=Nanum+Myeongjo:wght@400;700;800&family=Noto+Sans+KR:wght@300;400;500;700&family=Noto+Serif+KR:wght@300;400;600;700&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet">
    
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* Custom Scrollbar Styling */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }
        ::-webkit-scrollbar-track {
            background: rgba(0, 0, 0, 0.05);
        }
        ::-webkit-scrollbar-thumb {
            background: rgba(0, 0, 0, 0.2);
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: rgba(0, 0, 0, 0.3);
        }

        /* Paper Spine Crease Effect */
        .spine-crease-left {
            background: linear-gradient(to right, rgba(0,0,0,0) 0%, rgba(0,0,0,0.03) 93%, rgba(0,0,0,0.12) 100%);
        }
        .spine-crease-right {
            background: linear-gradient(to left, rgba(0,0,0,0) 0%, rgba(0,0,0,0.03) 93%, rgba(0,0,0,0.12) 100%);
        }

        /* Long press hold progress animation */
        @keyframes pulse-ring {
            0% { transform: scale(0.95); opacity: 0.8; }
            50% { transform: scale(1.05); opacity: 1; }
            100% { transform: scale(0.95); opacity: 0.8; }
        }
        .pressing-active {
            animation: pulse-ring 0.4s infinite ease-in-out;
            border-color: #ef4444 !important;
            background-color: #fee2e2 !important;
            color: #dc2626 !important;
        }

        /* Print Margin Dashed Box Overlay */
        .margin-guide-box {
            border: 1px dashed rgba(59, 130, 246, 0.45);
            pointer-events: none;
        }

        /* Mind map grid canvas */
        .mindmap-bg {
            background-image: radial-gradient(circle, rgba(156, 163, 175, 0.25) 1px, transparent 1px);
            background-size: 24px 24px;
        }

        /* Paragraph Indentation & Spacing Engine */
        .book-editor-area p, .book-editor-area div {
            margin-top: 0px !important;
            margin-bottom: var(--para-spacing, 0px) !important;
            text-indent: var(--text-indent, 1.0em) !important;
            line-height: inherit;
        }
        .book-editor-area p:empty::before, .book-editor-area div:empty::before {
            content: "\00a0"; /* Non-breaking space for empty paragraphs */
        }
        /* 개별 문단 들여쓰기 제거용 클래스 */
        .book-editor-area p.no-indent, .book-editor-area div.no-indent {
            text-indent: 0 !important;
        }

        /* Mind map cursor and mode isolation rules */
        .mode-pan-active .mindmap-node-card {
            cursor: grab !important;
        }
        .mode-node-active .mindmap-node-card {
            cursor: move !important;
            box-shadow: 0 10px 25px -5px rgba(59, 130, 246, 0.3), 0 8px 10px -6px rgba(59, 130, 246, 0.2);
        }
        .mode-connect-active .mindmap-node-card {
            cursor: crosshair !important;
        }
        .connection-group:hover circle {
            fill: #dc2626 !important;
            transform: scale(1.25);
            transform-origin: center;
        }
        .connection-group:hover line:first-child {
            stroke: #ef4444 !important;
            stroke-width: 3.5px !important;
        }

        /* Accordion transition styles */
        .accordion-content {
            transition: max-height 0.25s ease-in-out, opacity 0.2s ease-in-out;
        }
    </style>
</head>
<body class="h-full bg-gray-100 dark:bg-gray-900 text-gray-800 dark:text-gray-100 flex flex-col font-gothic overflow-hidden select-none">

    <!-- TOP APP NAVIGATION BAR -->
    <header class="h-14 bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 px-4 flex items-center justify-between shrink-0 z-30 shadow-sm">
        <div class="flex items-center space-x-3 sm:space-x-4">
            <!-- Brand Logo -->
            <div class="flex items-center space-x-2">
                <div class="w-8 h-8 rounded-lg bg-gradient-to-tr from-brand-600 to-indigo-600 flex items-center justify-center text-white shadow-md">
                    <i class="fa-solid fa-book-bookmark text-sm"></i>
                </div>
                <span class="font-bold text-base tracking-tight hidden sm:inline-block">BookCraft <span class="text-brand-600 dark:text-brand-400 font-mono text-xs px-1.5 py-0.5 rounded bg-brand-50 dark:bg-gray-700">Studio</span></span>
            </div>

            <div class="h-5 w-px bg-gray-300 dark:bg-gray-600"></div>

            <!-- Workspace Selector & Rename Action -->
            <div class="flex items-center space-x-1">
                <i class="fa-solid fa-folder-closed text-xs text-gray-400"></i>
                <select id="workspace-select" onchange="switchWorkspace(this.value)" class="bg-gray-50 dark:bg-gray-700 text-xs font-semibold rounded-md border border-gray-200 dark:border-gray-600 px-2 py-1 outline-none focus:ring-1 focus:ring-brand-500 max-w-[120px] sm:max-w-[180px]">
                    <!-- Dynamic Workspaces Injected -->
                </select>
                <button onclick="openRenameWorkspaceModal()" title="현재 원고 제목 변경" class="p-1 hover:bg-gray-100 dark:hover:bg-gray-700 text-gray-600 dark:text-gray-300 rounded text-xs transition">
                    <i class="fa-solid fa-pen text-xs"></i>
                </button>
                <button onclick="openNewWorkspaceModal()" title="새 워크스페이스 추가" class="p-1 hover:bg-gray-100 dark:hover:bg-gray-700 text-gray-600 dark:text-gray-300 rounded text-xs transition">
                    <i class="fa-solid fa-plus"></i>
                </button>
                <button onclick="confirmDeleteCurrentWorkspace()" title="현재 워크스페이스 삭제" class="p-1 hover:bg-red-50 dark:hover:bg-red-900/30 text-gray-400 hover:text-red-600 rounded text-xs transition">
                    <i class="fa-regular fa-trash-can"></i>
                </button>
            </div>
        </div>

        <!-- Center Workspace View Tabs -->
        <div class="flex items-center bg-gray-100 dark:bg-gray-700 p-1 rounded-lg border border-gray-200 dark:border-gray-600 text-xs font-semibold">
            <button id="tab-studio-btn" onclick="switchMainTab('studio')" class="px-3 py-1 rounded-md transition shadow-sm bg-white dark:bg-gray-800 text-brand-600 dark:text-brand-400">
                <i class="fa-solid fa-file-pen mr-1"></i>조판 & 원고
            </button>
            <button id="tab-mindmap-btn" onclick="switchMainTab('mindmap')" class="px-3 py-1 rounded-md transition text-gray-600 dark:text-gray-300 hover:text-gray-900">
                <i class="fa-solid fa-diagram-project mr-1"></i>마인드맵
            </button>
        </div>

        <!-- Right Side Actions & Save Button -->
        <div class="flex items-center space-x-2">
            <!-- Sync Status Indicator Badge -->
            <div id="sync-status" class="hidden lg:flex items-center space-x-1.5 text-[11px] text-gray-500 dark:text-gray-400 px-2.5 py-1 bg-gray-50 dark:bg-gray-700/60 rounded-md border border-gray-200 dark:border-gray-600">
                <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span>
                <span id="sync-status-text">자동 저장: 10분 주기</span>
            </div>

            <!-- PROMINENT SAVE NOW BUTTON -->
            <button onclick="manualSaveNow()" class="px-3.5 py-1.5 bg-gradient-to-r from-emerald-600 to-teal-600 hover:from-emerald-700 hover:to-teal-700 active:scale-95 text-white rounded-lg text-xs font-bold flex items-center space-x-1.5 shadow-md shadow-emerald-500/20 transition-all duration-150">
                <i class="fa-solid fa-floppy-disk text-sm"></i>
                <span class="hidden sm:inline">지금 저장</span>
            </button>

            <!-- EXPORT MANUSCRIPT BUTTON -->
            <button onclick="openExportModal()" title="원고 내보내기" class="px-2.5 py-1.5 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 rounded-lg text-xs font-medium flex items-center space-x-1 transition">
                <i class="fa-solid fa-file-export text-sm"></i>
                <span class="hidden md:inline">내보내기</span>
            </button>

            <!-- User Auth Modal Trigger -->
            <button id="auth-btn" onclick="openAuthModal()" class="px-2.5 py-1.5 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 rounded-lg text-xs font-medium flex items-center space-x-1 transition">
                <i class="fa-regular fa-circle-user text-sm"></i>
                <span id="auth-btn-text" class="hidden sm:inline">로그인 / 회원가입</span>
            </button>

            <!-- Dark Mode Toggle -->
            <button onclick="toggleDarkMode()" class="p-1.5 text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-lg text-xs transition" title="다크 모드 전환">
                <i class="fa-solid fa-moon dark:hidden"></i>
                <i class="fa-solid fa-sun hidden dark:inline"></i>
            </button>
        </div>
    </header>

    <!-- MAIN WORKSPACE CONTENT CONTAINER -->
    <main class="flex-1 flex overflow-hidden relative">

        <!-- TAB 1: STUDIO (BOOK LAYOUT & TYPOGRAPHY) -->
        <div id="view-studio" class="flex-1 flex w-full h-full relative">

            <!-- LEFT CONTROL PANEL (BOOK SETTINGS WITH ACCORDION SECTIONS) -->
            <aside id="sidebar-panel" class="w-80 bg-white dark:bg-gray-800 border-r border-gray-200 dark:border-gray-700 flex flex-col shrink-0 z-20 transition-all duration-300">
                
                <div class="p-3 border-b border-gray-200 dark:border-gray-700 flex items-center justify-between bg-gray-50/50 dark:bg-gray-800/50">
                    <span class="text-xs font-bold text-gray-600 dark:text-gray-300 uppercase tracking-wider">
                        <i class="fa-solid fa-sliders mr-1 text-brand-500"></i> 조판 & 서식 설정
                    </span>
                    <div class="flex items-center space-x-1">
                        <button onclick="resetBookSettings()" title="설정 초기화" class="px-1.5 py-0.5 text-[11px] text-gray-400 hover:text-gray-600 dark:hover:text-gray-200">
                            <i class="fa-solid fa-rotate-right mr-1"></i>초기화
                        </button>
                        <button onclick="toggleSidebar()" title="사이드바 접기" class="px-1.5 py-0.5 text-[11px] bg-gray-200/50 dark:bg-gray-700/50 hover:bg-gray-200 dark:hover:bg-gray-600 text-gray-600 dark:text-gray-300 rounded transition">
                            <i class="fa-solid fa-chevron-left mr-1"></i>접기
                        </button>
                    </div>
                </div>

                <div class="flex-1 overflow-y-auto divide-y divide-gray-100 dark:divide-gray-700/60 text-xs">

                    <!-- ACCORDION SECTION 1: 책 판형 및 크기 설정 -->
                    <div class="accordion-item">
                        <button onclick="toggleAccordion('acc-preset')" class="w-full p-3 flex items-center justify-between font-bold text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-750 transition text-left">
                            <span class="flex items-center space-x-2">
                                <i class="fa-solid fa-book-open text-brand-500"></i>
                                <span>1. 책 판형 (Book Preset)</span>
                            </span>
                            <i id="acc-preset-icon" class="fa-solid fa-chevron-down text-gray-400 text-xs transition-transform duration-200"></i>
                        </button>
                        <div id="acc-preset" class="accordion-content p-3 pt-0 space-y-2">
                            <select id="setting-preset" onchange="applyBookPreset(this.value)" class="w-full bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-lg p-2 outline-none focus:ring-1 focus:ring-brand-500 font-medium">
                                <option value="sin-guk">신국판 (152 x 225 mm) - 표준 소설</option>
                                <option value="a5">A5 (148 x 210 mm) - 소설/수필</option>
                                <option value="b6">B6 / 사륙판 (128 x 182 mm) - 시집/단편</option>
                                <option value="pocket">문고판 (105 x 148 mm) - 손안의 책</option>
                                <option value="custom">직접 입력 (Custom Size)</option>
                            </select>

                            <!-- Custom Width / Height Inputs -->
                            <div id="custom-size-box" class="grid grid-cols-2 gap-2 pt-1 hidden">
                                <div>
                                    <span class="text-[11px] text-gray-400">가로 (Width mm)</span>
                                    <input type="number" id="setting-width" min="80" max="300" onchange="updateCustomSize()" class="w-full mt-0.5 bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded p-1.5 font-mono">
                                </div>
                                <div>
                                    <span class="text-[11px] text-gray-400">세로 (Height mm)</span>
                                    <input type="number" id="setting-height" min="100" max="400" onchange="updateCustomSize()" class="w-full mt-0.5 bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded p-1.5 font-mono">
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- ACCORDION SECTION 2: 펼침 모드 선택 (Spread vs Single) -->
                    <div class="accordion-item">
                        <button onclick="toggleAccordion('acc-view')" class="w-full p-3 flex items-center justify-between font-bold text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-750 transition text-left">
                            <span class="flex items-center space-x-2">
                                <i class="fa-solid fa-table-columns text-brand-500"></i>
                                <span>2. 보기 형태 (View Mode)</span>
                            </span>
                            <i id="acc-view-icon" class="fa-solid fa-chevron-down text-gray-400 text-xs transition-transform duration-200"></i>
                        </button>
                        <div id="acc-view" class="accordion-content p-3 pt-0 space-y-2">
                            <div class="grid grid-cols-2 gap-2">
                                <button id="btn-mode-single" onclick="setSpreadMode(false)" class="p-2 border rounded-lg font-semibold flex flex-col items-center space-y-1 transition border-brand-500 bg-brand-50 dark:bg-brand-900/30 text-brand-600 dark:text-brand-300">
                                    <i class="fa-solid fa-file-lines text-base"></i>
                                    <span>단면 보기</span>
                                </button>
                                <button id="btn-mode-spread" onclick="setSpreadMode(true)" class="p-2 border rounded-lg font-semibold flex flex-col items-center space-y-1 transition border-gray-200 dark:border-gray-600 text-gray-500">
                                    <i class="fa-solid fa-book-open text-base"></i>
                                    <span>양면 펼침</span>
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- ACCORDION SECTION 3: 상하좌우 여백 및 제본 반전 설정 (Margins) -->
                    <div class="accordion-item">
                        <button onclick="toggleAccordion('acc-margin')" class="w-full p-3 flex items-center justify-between font-bold text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-750 transition text-left">
                            <span class="flex items-center space-x-2">
                                <i class="fa-solid fa-ruler-combined text-brand-500"></i>
                                <span>3. 인쇄 여백 & 제본 설정</span>
                            </span>
                            <i id="acc-margin-icon" class="fa-solid fa-chevron-down text-gray-400 text-xs transition-transform duration-200"></i>
                        </button>
                        <div id="acc-margin" class="accordion-content p-3 pt-0 space-y-3">
                            <div class="flex items-center justify-between">
                                <span class="text-[11px] text-gray-500 dark:text-gray-400">인쇄 영역 가이드</span>
                                <label class="inline-flex items-center cursor-pointer">
                                    <input type="checkbox" id="setting-margin-guide" onchange="toggleMarginGuide(this.checked)" checked class="sr-only peer">
                                    <div class="w-7 h-4 bg-gray-200 peer-focus:outline-none rounded-full peer dark:bg-gray-700 peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-3 after:w-3 after:transition-all peer-checked:bg-brand-600 relative"></div>
                                    <span class="ml-1 text-[10px] text-gray-400">표시</span>
                                </label>
                            </div>

                            <div class="grid grid-cols-2 gap-2 font-mono">
                                <div>
                                    <span class="text-[10px] text-gray-400">상단 (Top mm)</span>
                                    <input type="number" id="setting-margin-top" value="20" min="5" max="60" oninput="updateTypographySettings()" class="w-full bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded p-1.5 text-center">
                                </div>
                                <div>
                                    <span class="text-[10px] text-gray-400">하단 (Bottom mm)</span>
                                    <input type="number" id="setting-margin-bottom" value="22" min="5" max="60" oninput="updateTypographySettings()" class="w-full bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded p-1.5 text-center">
                                </div>
                                <div>
                                    <span class="text-[10px] text-gray-400">바깥쪽 (Left mm)</span>
                                    <input type="number" id="setting-margin-left" value="18" min="5" max="60" oninput="updateTypographySettings()" class="w-full bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded p-1.5 text-center">
                                </div>
                                <div>
                                    <span class="text-[10px] text-gray-400">안쪽/제본 (Right mm)</span>
                                    <input type="number" id="setting-margin-right" value="18" min="5" max="60" oninput="updateTypographySettings()" class="w-full bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded p-1.5 text-center">
                                </div>
                            </div>

                            <div class="bg-blue-50/70 dark:bg-blue-900/20 p-2 rounded border border-blue-200 dark:border-blue-800/40 text-[10px] text-blue-700 dark:text-blue-300 flex items-start space-x-1.5">
                                <i class="fa-solid fa-arrows-left-right text-xs mt-0.5 shrink-0"></i>
                                <span>짝수 페이지는 안쪽(제본)과 바깥쪽 여백이 자동으로 좌우 반전 적용됩니다.</span>
                            </div>
                        </div>
                    </div>

                    <!-- ACCORDION SECTION 4: 서체 및 본문 스타일 (Typography) -->
                    <div class="accordion-item">
                        <button onclick="toggleAccordion('acc-typo')" class="w-full p-3 flex items-center justify-between font-bold text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-750 transition text-left">
                            <span class="flex items-center space-x-2">
                                <i class="fa-solid fa-font text-brand-500"></i>
                                <span>4. 서체 및 스타일</span>
                            </span>
                            <i id="acc-typo-icon" class="fa-solid fa-chevron-down text-gray-400 text-xs transition-transform duration-200"></i>
                        </button>
                        <div id="acc-typo" class="accordion-content p-3 pt-0 space-y-3">
                            <!-- Font Family -->
                            <div>
                                <span class="text-[11px] text-gray-400">폰트 종류</span>
                                <select id="setting-font-family" onchange="updateTypographySettings()" class="w-full mt-0.5 bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-lg p-2 outline-none">
                                    <option value="'Gowun Batang', serif">고운바탕 (Gowun Batang)</option>
                                    <option value="'Nanum Myeongjo', serif">나눔명조 (Nanum Myeongjo)</option>
                                    <option value="'Noto Serif KR', serif">Noto Serif KR (표준 명조)</option>
                                    <option value="'Noto Sans KR', sans-serif">Noto Sans KR (고딕)</option>
                                </select>
                            </div>

                            <!-- Font Size & Line Height -->
                            <div class="grid grid-cols-2 gap-2">
                                <div>
                                    <div class="flex justify-between text-[11px]">
                                        <span class="text-gray-400">글자 크기</span>
                                        <span id="label-font-size" class="font-mono font-bold text-brand-600">10.5pt</span>
                                    </div>
                                    <input type="range" id="setting-font-size" min="8" max="18" step="0.5" value="10.5" oninput="updateTypographySettings()" class="w-full accent-brand-600 mt-1">
                                </div>

                                <div>
                                    <div class="flex justify-between text-[11px]">
                                        <span class="text-gray-400">줄 간격</span>
                                        <span id="label-line-height" class="font-mono font-bold text-brand-600">1.75</span>
                                    </div>
                                    <input type="range" id="setting-line-height" min="1.0" max="3.0" step="0.05" value="1.75" oninput="updateTypographySettings()" class="w-full accent-brand-600 mt-1">
                                </div>
                            </div>

                            <!-- Letter Spacing -->
                            <div>
                                <div class="flex justify-between text-[11px]">
                                    <span class="text-gray-400">자간 (Letter Spacing)</span>
                                    <span id="label-letter-spacing" class="font-mono font-bold text-brand-600">0px</span>
                                </div>
                                <input type="range" id="setting-letter-spacing" min="-2" max="5" step="0.5" value="0" oninput="updateTypographySettings()" class="w-full accent-brand-600 mt-1">
                            </div>

                            <!-- First-line Indent & Paragraph Spacing Controls -->
                            <div class="grid grid-cols-2 gap-2">
                                <div>
                                    <div class="flex justify-between text-[11px]">
                                        <span class="text-gray-400">들여쓰기 (Indent)</span>
                                        <span id="label-text-indent" class="font-mono font-bold text-brand-600">1.0em</span>
                                    </div>
                                    <input type="range" id="setting-text-indent" min="0" max="3" step="0.5" value="1" oninput="updateTypographySettings()" class="w-full accent-brand-600 mt-1">
                                </div>

                                <div>
                                    <div class="flex justify-between text-[11px]">
                                        <span class="text-gray-400">내려쓰기/문단 간격</span>
                                        <span id="label-paragraph-spacing" class="font-mono font-bold text-brand-600">0px</span>
                                    </div>
                                    <input type="range" id="setting-paragraph-spacing" min="0" max="30" step="1" value="0" oninput="updateTypographySettings()" class="w-full accent-brand-600 mt-1">
                                </div>
                            </div>

                            <!-- Text Color & Paper Color -->
                            <div class="grid grid-cols-2 gap-2 pt-1">
                                <div>
                                    <span class="text-[11px] text-gray-400 block mb-1">글자 색상</span>
                                    <div class="flex items-center space-x-2">
                                        <input type="color" id="setting-text-color" value="#111827" oninput="updateTypographySettings()" class="w-8 h-8 rounded border-none cursor-pointer bg-transparent">
                                        <span id="label-text-color" class="font-mono text-[10px] text-gray-500">#111827</span>
                                    </div>
                                </div>

                                <div>
                                    <span class="text-[11px] text-gray-400 block mb-1">종이 바탕색</span>
                                    <div class="flex items-center space-x-1">
                                        <button onclick="setPaperColor('#ffffff')" class="w-6 h-6 rounded border border-gray-300 bg-white" title="순백색"></button>
                                        <button onclick="setPaperColor('#fdfbf7')" class="w-6 h-6 rounded border border-gray-300 bg-[#fdfbf7]" title="미색"></button>
                                        <button onclick="setPaperColor('#f5efe0')" class="w-6 h-6 rounded border border-gray-300 bg-[#f5efe0]" title="빈티지"></button>
                                        <input type="color" id="setting-paper-color" value="#fdfbf7" oninput="updateTypographySettings()" class="w-6 h-6 rounded border-none cursor-pointer bg-transparent" title="사용자 색상">
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- ACCORDION SECTION 5: 조판 수치 분석 통계 (Analytics) -->
                    <div class="accordion-item">
                        <button onclick="toggleAccordion('acc-stats')" class="w-full p-3 flex items-center justify-between font-bold text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-750 transition text-left">
                            <span class="flex items-center space-x-2">
                                <i class="fa-solid fa-chart-simple text-brand-500"></i>
                                <span>5. 실시간 조판 분석</span>
                            </span>
                            <i id="acc-stats-icon" class="fa-solid fa-chevron-down text-gray-400 text-xs transition-transform duration-200"></i>
                        </button>
                        <div id="acc-stats" class="accordion-content p-3 pt-0">
                            <div class="bg-gray-50 dark:bg-gray-700/50 p-3 rounded-lg space-y-2 border border-gray-200/80 dark:border-gray-600">
                                <div class="grid grid-cols-2 gap-2 text-[11px]">
                                    <div class="bg-white dark:bg-gray-800 p-2 rounded shadow-xs">
                                        <span class="text-gray-400 block">줄당 자수 (예상)</span>
                                        <span id="stat-chars-per-line" class="font-bold text-sm text-gray-800 dark:text-gray-100 font-mono">약 32 자</span>
                                    </div>
                                    <div class="bg-white dark:bg-gray-800 p-2 rounded shadow-xs">
                                        <span class="text-gray-400 block">페이지당 줄 수</span>
                                        <span id="stat-lines-per-page" class="font-bold text-sm text-gray-800 dark:text-gray-100 font-mono">약 22 줄</span>
                                    </div>
                                    <div class="bg-white dark:bg-gray-800 p-2 rounded shadow-xs">
                                        <span class="text-gray-400 block">페이지당 자수</span>
                                        <span id="stat-chars-per-page" class="font-bold text-sm text-gray-800 dark:text-gray-100 font-mono">약 700 자</span>
                                    </div>
                                    <div class="bg-white dark:bg-gray-800 p-2 rounded shadow-xs">
                                        <span class="text-gray-400 block">200자 원고지 환산</span>
                                        <span id="stat-manuscript-pages" class="font-bold text-sm text-brand-600 font-mono">0 매</span>
                                    </div>
                                </div>

                                <div class="text-[10px] text-gray-400 pt-1 flex justify-between">
                                    <span>전체 글자: <strong id="stat-total-chars" class="text-gray-600 dark:text-gray-300 font-mono">0</strong>자</span>
                                    <span>단어: <strong id="stat-total-words" class="text-gray-600 dark:text-gray-300 font-mono">0</strong>개</span>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
            </aside>

            <!-- 플로팅 화살표 버튼 (사이드바 펼침용) -->
            <button id="btn-expand-sidebar" onclick="toggleSidebar()" class="absolute left-0 top-1/2 -translate-y-1/2 z-30 hidden bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 border-l-0 rounded-r-xl py-4 px-2 shadow-lg text-gray-400 hover:text-brand-600 transition-all">
                <i class="fa-solid fa-chevron-right"></i>
            </button>

            <!-- CENTER CANVAS AREA (VIRTUAL BOOK PREVIEW) -->
            <div class="flex-1 flex flex-col bg-gray-200/80 dark:bg-gray-950 overflow-hidden relative">
                
                <!-- Canvas Scale Header Controls -->
                <div class="h-10 bg-white/70 dark:bg-gray-800/70 backdrop-blur border-b border-gray-200 dark:border-gray-700 px-4 flex items-center justify-between shrink-0 z-10 text-xs">
                    <div class="flex items-center space-x-2">
                        <button onclick="toggleSidebar()" class="p-1 hover:bg-gray-200 dark:hover:bg-gray-700 rounded text-gray-600 dark:text-gray-300 transition" title="사이드바 토글">
                            <i class="fa-solid fa-sidebar"></i>
                        </button>
                        <span id="canvas-page-info" class="font-semibold text-gray-700 dark:text-gray-300">
                            Page 1 / 1
                        </span>
                    </div>

                    <!-- Page Management Actions -->
                    <div class="flex items-center space-x-2">
                        <!-- 선택 문단 들여쓰기 켜기/끄기 추가 -->
                        <button onclick="toggleParagraphIndent()" class="px-2.5 py-1.5 bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 rounded-lg text-xs font-semibold flex items-center space-x-1 shadow-sm transition" title="현재 커서가 있는 문단의 들여쓰기 여부를 전환합니다.">
                            <i class="fa-solid fa-indent"></i>
                            <span class="hidden sm:inline">들여쓰기 켜기/끄기</span>
                        </button>
                        <button onclick="addNewPage()" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-700 text-white rounded-lg text-xs font-semibold flex items-center space-x-1 shadow-sm transition">
                            <i class="fa-solid fa-plus text-[10px]"></i>
                            <span>새 페이지 추가</span>
                        </button>
                    </div>

                    <!-- Zoom & Scale Bar -->
                    <div class="flex items-center space-x-2">
                        <button onclick="adjustScale(-0.1)" class="p-1 text-gray-500 hover:text-gray-800"><i class="fa-solid fa-minus"></i></button>
                        <span id="scale-label" class="font-mono text-gray-600 dark:text-gray-400 w-12 text-center">100%</span>
                        <button onclick="adjustScale(0.1)" class="p-1 text-gray-500 hover:text-gray-800"><i class="fa-solid fa-plus"></i></button>
                        <button onclick="setScale(1.0)" class="text-[10px] text-gray-400 hover:text-brand-600 ml-1">100% 리셋</button>
                    </div>
                </div>

                <!-- VIRTUAL BOOK SHEET SCROLLABLE VIEWPORT -->
                <div id="book-viewport" class="flex-1 overflow-auto p-8 flex items-center justify-center">
                    
                    <!-- DYNAMIC VIRTUAL BOOK CONTAINER -->
                    <div id="book-sheet-container" class="transition-all duration-200 ease-out flex items-center justify-center drop-shadow-2xl my-auto">
                        <!-- Dynamic Pages HTML Rendered Here -->
                    </div>

                </div>

                <!-- Quick Page Thumbnails Strip & Search Bar -->
                <div class="bg-white/80 dark:bg-gray-800/80 backdrop-blur border-t border-gray-200 dark:border-gray-700 px-4 py-2 flex items-center justify-between shrink-0 z-10 space-x-2">
                    <div id="page-thumbnails-strip" class="flex items-center space-x-2 overflow-x-auto flex-1 pb-1">
                        <!-- Dynamic Thumbnail buttons injected here -->
                    </div>
                    
                    <!-- Page Jump / Search Widget -->
                    <div class="flex items-center space-x-1.5 shrink-0 bg-gray-100 dark:bg-gray-700 p-1.5 rounded-lg border border-gray-200 dark:border-gray-600">
                        <i class="fa-solid fa-magnifying-glass text-[10px] text-gray-500 dark:text-gray-400 ml-1"></i>
                        <input type="number" id="jump-page-input" placeholder="Pg #" min="1" onkeyup="if(event.key === 'Enter') jumpToPage()" class="w-12 px-1 py-1 text-xs text-center border-none bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-200 rounded outline-none focus:ring-1 focus:ring-brand-500 shadow-sm font-mono">
                        <button onclick="jumpToPage()" class="px-2 py-1 text-[11px] font-bold bg-brand-600 hover:bg-brand-700 text-white rounded transition shadow-sm">이동</button>
                    </div>
                </div>

                <!-- Bottom Typography Analytics Bar -->
                <div class="h-6 bg-gray-100 dark:bg-gray-800 border-t border-gray-200 dark:border-gray-700 px-4 flex items-center justify-between text-[11px] text-gray-500 dark:text-gray-400 shrink-0">
                    <div class="flex items-center space-x-3">
                        <span id="info-dimension-badge">신국판 (152 × 225 mm)</span>
                        <span>•</span>
                        <span id="info-spread-badge">단면 편집 모드</span>
                    </div>
                    <div class="flex items-center space-x-2 text-[10px]">
                        <span class="text-gray-400"><i class="fa-solid fa-circle-info mr-1"></i>페이지 썸네일을 <strong>700ms 길게 누르면</strong> 삭제 모드가 호출됩니다.</span>
                    </div>
                </div>

            </div>
        </div>

        <!-- TAB 2: STORY MIND MAP STUDIO (ENHANCED ENGINE) -->
        <div id="view-mindmap" class="flex-1 flex flex-col w-full h-full hidden bg-gray-950 relative overflow-hidden">
            
            <!-- Mindmap Dynamic Mode Toolbar -->
            <div class="h-14 bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 px-4 flex items-center justify-between shrink-0 z-20 shadow-sm">
                <!-- Add Node Type Category Buttons -->
                <div class="flex items-center space-x-1.5 overflow-x-auto py-1">
                    <span class="font-bold text-xs text-gray-700 dark:text-gray-200 mr-1 shrink-0">
                        <i class="fa-solid fa-plus text-brand-500 mr-1"></i>노드 추가:
                    </span>
                    <button onclick="addMindMapNode('character')" class="px-2.5 py-1 bg-amber-500/10 hover:bg-amber-500/20 text-amber-600 dark:text-amber-400 border border-amber-500/30 rounded-md text-xs font-semibold shrink-0 flex items-center space-x-1 transition">
                        <i class="fa-solid fa-user text-[10px]"></i><span>인물</span>
                    </button>
                    <button onclick="addMindMapNode('event')" class="px-2.5 py-1 bg-blue-500/10 hover:bg-blue-500/20 text-blue-600 dark:text-blue-400 border border-blue-500/30 rounded-md text-xs font-semibold shrink-0 flex items-center space-x-1 transition">
                        <i class="fa-solid fa-bolt text-[10px]"></i><span>사건</span>
                    </button>
                    <button onclick="addMindMapNode('plot')" class="px-2.5 py-1 bg-emerald-500/10 hover:bg-emerald-500/20 text-emerald-600 dark:text-emerald-400 border border-emerald-500/30 rounded-md text-xs font-semibold shrink-0 flex items-center space-x-1 transition">
                        <i class="fa-solid fa-route text-[10px]"></i><span>플롯</span>
                    </button>
                    <button onclick="addMindMapNode('clue')" class="px-2.5 py-1 bg-purple-500/10 hover:bg-purple-500/20 text-purple-600 dark:text-purple-400 border border-purple-500/30 rounded-md text-xs font-semibold shrink-0 flex items-center space-x-1 transition">
                        <i class="fa-solid fa-key text-[10px]"></i><span>복선</span>
                    </button>
                    <button onclick="addMindMapNode('setting')" class="px-2.5 py-1 bg-rose-500/10 hover:bg-rose-500/20 text-rose-600 dark:text-rose-400 border border-rose-500/30 rounded-md text-xs font-semibold shrink-0 flex items-center space-x-1 transition">
                        <i class="fa-solid fa-compass text-[10px]"></i><span>배경</span>
                    </button>
                </div>

                <!-- ISOLATED MODE SELECTOR GROUP (Pan vs Node Drag vs Connect) -->
                <div class="flex items-center space-x-3 shrink-0">
                    <div class="flex items-center bg-gray-100 dark:bg-gray-700/80 p-1 rounded-lg border border-gray-200 dark:border-gray-600 text-xs font-bold shadow-inner">
                        <button id="mm-mode-pan" onclick="setMindMapMode('pan')" class="px-3 py-1 rounded-md transition flex items-center space-x-1.5 bg-brand-600 text-white shadow">
                            <i class="fa-solid fa-hand"></i>
                            <span>캔버스 이동</span>
                        </button>
                        
                        <button id="mm-mode-node" onclick="setMindMapMode('node')" class="px-3 py-1 rounded-md transition flex items-center space-x-1.5 text-gray-600 dark:text-gray-300">
                            <i class="fa-solid fa-up-down-left-right"></i>
                            <span>노드 이동 모드</span>
                        </button>
                        
                        <button id="mm-mode-connect" onclick="setMindMapMode('connect')" class="px-3 py-1 rounded-md transition flex items-center space-x-1.5 text-gray-600 dark:text-gray-300">
                            <i class="fa-solid fa-link"></i>
                            <span>노드 연결</span>
                        </button>
                    </div>

                    <!-- Canvas Zoom Controls & Clear -->
                    <div class="flex items-center space-x-1.5 bg-gray-100 dark:bg-gray-700/80 p-1 rounded-lg border border-gray-200 dark:border-gray-600">
                        <button onclick="zoomMindMap(-0.1)" class="w-6 h-6 flex items-center justify-center hover:bg-gray-200 dark:hover:bg-gray-600 rounded text-gray-600 dark:text-gray-300"><i class="fa-solid fa-minus text-[10px]"></i></button>
                        <span id="mm-zoom-label" class="text-[11px] font-mono font-bold w-10 text-center text-gray-700 dark:text-gray-200">100%</span>
                        <button onclick="zoomMindMap(0.1)" class="w-6 h-6 flex items-center justify-center hover:bg-gray-200 dark:hover:bg-gray-600 rounded text-gray-600 dark:text-gray-300"><i class="fa-solid fa-plus text-[10px]"></i></button>
                        <button onclick="resetMindMapTransform()" title="뷰 초기화" class="p-1 hover:bg-gray-200 dark:hover:bg-gray-600 rounded text-gray-400 hover:text-gray-700 dark:hover:text-gray-200 text-xs"><i class="fa-solid fa-rotate-left"></i></button>
                    </div>

                    <button onclick="clearMindMap()" class="p-1.5 text-gray-400 hover:text-red-500 rounded transition" title="마인드맵 초기화">
                        <i class="fa-solid fa-trash-can"></i>
                    </button>
                </div>
            </div>

            <!-- Mindmap Infinite Pointer Canvas -->
            <div id="mindmap-canvas" class="flex-1 w-full h-full relative mindmap-bg overflow-hidden touch-none select-none mode-pan-active cursor-grab">
                
                <!-- Transform Viewport Layer -->
                <div id="mindmap-transform-layer" class="absolute inset-0 origin-top-left transition-transform duration-75">
                    <!-- SVG Connections Overlay (z-10: BEHIND Nodes Layer) -->
                    <svg id="mindmap-svg-layer" class="absolute inset-0 w-[5000px] h-[5000px] pointer-events-none z-10">
                        <defs>
                            <marker id="arrow" viewBox="0 0 10 10" refX="22" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
                                <path d="M 0 0 L 10 5 L 0 10 z" fill="#3b82f6"/>
                            </marker>
                            <marker id="arrow-preview" viewBox="0 0 10 10" refX="10" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
                                <path d="M 0 0 L 10 5 L 0 10 z" fill="#f59e0b"/>
                            </marker>
                        </defs>
                        <g id="svg-connections-group"></g>
                        <line id="svg-preview-line" x1="0" y1="0" x2="0" y2="0" stroke="#f59e0b" stroke-width="2" stroke-dasharray="4 4" marker-end="url(#arrow-preview)" class="hidden" />
                    </svg>

                    <!-- Dynamic Draggable Nodes Container (z-20: IN FRONT of Lines) -->
                    <div id="mindmap-nodes-layer" class="absolute inset-0 z-20 pointer-events-none"></div>
                </div>

            </div>

            <!-- Floating Mode Guidance Banner -->
            <div id="mm-status-banner" class="absolute bottom-4 left-4 bg-gray-900/90 backdrop-blur-md text-gray-200 text-xs px-3.5 py-2 rounded-xl border border-gray-700 shadow-xl pointer-events-none flex items-center space-x-2 z-30">
                <i class="fa-solid fa-circle-info text-brand-400"></i>
                <span id="mm-status-text"><strong>🖐️ 캔버스 이동 모드:</strong> 바탕화면이나 노드 위를 잡고 드래그하여 화면 위치를 이동하세요. (노드는 고정됩니다)</span>
            </div>

        </div>

    </main>

    <!-- RENAME WORKSPACE MODAL -->
    <div id="modal-rename-workspace" class="fixed inset-0 bg-black/60 backdrop-blur-xs flex items-center justify-center z-50 hidden p-4">
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-2xl border border-gray-200 dark:border-gray-700 max-w-sm w-full p-5 space-y-4">
            <div class="flex items-center space-x-2 text-brand-600 dark:text-brand-400 font-bold text-sm">
                <i class="fa-solid fa-pen-to-square"></i>
                <span>원고(작품) 제목 변경</span>
            </div>
            
            <div>
                <label class="block text-xs font-semibold text-gray-600 dark:text-gray-300 mb-1">새 작품 제목</label>
                <input type="text" id="rename-ws-title-input" onkeyup="if(event.key === 'Enter') confirmRenameWorkspace()" class="w-full bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-lg p-2.5 text-xs outline-none focus:ring-2 focus:ring-brand-500 font-medium text-gray-800 dark:text-gray-100">
            </div>

            <div class="flex items-center justify-end space-x-2 pt-1">
                <button onclick="closeRenameWorkspaceModal()" class="px-3.5 py-1.5 bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 rounded-lg text-xs font-semibold transition">
                    취소
                </button>
                <button onclick="confirmRenameWorkspace()" class="px-3.5 py-1.5 bg-brand-600 hover:bg-brand-700 text-white rounded-lg text-xs font-semibold transition shadow-sm">
                    제목 저장
                </button>
            </div>
        </div>
    </div>

    <!-- CUSTOM DELETE PAGE CONFIRMATION MODAL -->
    <div id="modal-delete-page" class="fixed inset-0 bg-black/60 backdrop-blur-xs flex items-center justify-center z-50 hidden p-4">
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-2xl border border-gray-200 dark:border-gray-700 max-w-sm w-full p-5 space-y-4 animate-in fade-in zoom-in duration-150">
            <div class="flex items-center space-x-3 text-red-600">
                <div class="w-10 h-10 rounded-full bg-red-100 dark:bg-red-900/40 flex items-center justify-center shrink-0">
                    <i class="fa-solid fa-triangle-exclamation text-lg"></i>
                </div>
                <div>
                    <h3 class="font-bold text-sm text-gray-900 dark:text-gray-100">페이지 삭제 확인</h3>
                    <p class="text-xs text-gray-500 dark:text-gray-400">선택한 원고 페이지를 삭제하시겠습니까?</p>
                </div>
            </div>
            
            <p id="delete-page-modal-text" class="text-xs bg-gray-50 dark:bg-gray-700/50 p-2.5 rounded-lg border border-gray-200 dark:border-gray-600 text-gray-700 dark:text-gray-300 font-mono">
                Page 1
            </p>

            <div class="flex items-center justify-end space-x-2 pt-2">
                <button onclick="closeDeletePageModal()" class="px-3.5 py-1.5 bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 rounded-lg text-xs font-semibold transition">
                    취소
                </button>
                <button id="confirm-delete-page-btn" class="px-3.5 py-1.5 bg-red-600 hover:bg-red-700 text-white rounded-lg text-xs font-semibold transition shadow-sm">
                    삭제하기
                </button>
            </div>
        </div>
    </div>

    <!-- NEW WORKSPACE MODAL -->
    <div id="modal-workspace" class="fixed inset-0 bg-black/60 backdrop-blur-xs flex items-center justify-center z-50 hidden p-4">
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-2xl border border-gray-200 dark:border-gray-700 max-w-md w-full p-6 space-y-4">
            <h3 class="font-bold text-base text-gray-900 dark:text-gray-100">새 소설 워크스페이스 만들기</h3>
            <div>
                <label class="block text-xs font-semibold text-gray-600 dark:text-gray-300 mb-1">작품/원고 제목</label>
                <input type="text" id="new-ws-title" placeholder="예: 달빛 도서관의 비밀" class="w-full bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-lg p-2.5 text-xs outline-none focus:ring-2 focus:ring-brand-500">
            </div>
            <div class="flex justify-end space-x-2 pt-2">
                <button onclick="closeWorkspaceModal()" class="px-4 py-2 bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 rounded-lg text-xs font-semibold">취소</button>
                <button onclick="createNewWorkspace()" class="px-4 py-2 bg-brand-600 hover:bg-brand-700 text-white rounded-lg text-xs font-semibold shadow-sm">생성하기</button>
            </div>
        </div>
    </div>

    <!-- AUTH MODAL (LOGIN & SIGN UP WITH EMAIL/PASSWORD) -->
    <div id="modal-auth" class="fixed inset-0 bg-black/60 backdrop-blur-xs flex items-center justify-center z-50 hidden p-4">
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-2xl border border-gray-200 dark:border-gray-700 max-w-sm w-full p-6 space-y-4">
            <div class="text-center space-y-1">
                <h3 id="auth-modal-title" class="font-bold text-base text-gray-900 dark:text-gray-100">계정 로그인 및 회원가입</h3>
                <p id="auth-modal-desc" class="text-xs text-gray-500 dark:text-gray-400">이메일 계정으로 로그인하여 내 원고를 안전하게 보관하세요.</p>
            </div>

            <!-- Login / Signup Form -->
            <div class="space-y-3 pt-2">
                <div>
                    <label class="block text-[11px] font-semibold text-gray-600 dark:text-gray-300 mb-1">이메일 주소</label>
                    <input type="email" id="auth-email-input" placeholder="example@email.com" class="w-full bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-lg p-2.5 text-xs outline-none focus:ring-2 focus:ring-brand-500 text-gray-800 dark:text-gray-100">
                </div>
                <div>
                    <label class="block text-[11px] font-semibold text-gray-600 dark:text-gray-300 mb-1">비밀번호 (6자리 이상)</label>
                    <input type="password" id="auth-password-input" placeholder="••••••••" class="w-full bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-lg p-2.5 text-xs outline-none focus:ring-2 focus:ring-brand-500 text-gray-800 dark:text-gray-100">
                </div>

                <div id="logged-in-panel" class="hidden space-y-2 text-center pt-2">
                    <p id="current-user-email-display" class="text-xs font-bold text-brand-600 dark:text-brand-400"></p>
                    <button onclick="handleLogout()" class="w-full py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg text-xs font-bold transition shadow-sm">
                        로그아웃하기
                    </button>
                </div>

                <div id="logged-out-panel" class="space-y-2 pt-2">
                    <button onclick="handleEmailLogin()" class="w-full py-2.5 bg-brand-600 hover:bg-brand-700 text-white rounded-lg text-xs font-bold shadow-md transition flex items-center justify-center space-x-2">
                        <i class="fa-solid fa-right-to-bracket"></i>
                        <span>로그인하기</span>
                    </button>
                    <button onclick="handleEmailRegister()" class="w-full py-2.5 bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 rounded-lg text-xs font-bold transition flex items-center justify-center space-x-2">
                        <i class="fa-solid fa-user-plus"></i>
                        <span>회원가입하기</span>
                    </button>
                </div>
            </div>

            <div class="flex justify-center pt-2 border-t border-gray-100 dark:border-gray-700">
                <button onclick="closeAuthModal()" class="text-xs text-gray-400 hover:text-gray-600">닫기</button>
            </div>
        </div>
    </div>

    <!-- EXPORT MANUSCRIPT MODAL -->
    <div id="modal-export" class="fixed inset-0 bg-black/60 backdrop-blur-xs flex items-center justify-center z-50 hidden p-4">
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-2xl border border-gray-200 dark:border-gray-700 max-w-lg w-full p-6 space-y-4">
            <div class="flex items-center justify-between border-b border-gray-200 dark:border-gray-700 pb-3">
                <h3 class="font-bold text-base text-gray-900 dark:text-gray-100 flex items-center space-x-2">
                    <i class="fa-solid fa-file-export text-brand-500"></i>
                    <span>전체 원고 내보내기</span>
                </h3>
                <button onclick="closeExportModal()" class="text-gray-400 hover:text-gray-600 dark:hover:text-gray-200"><i class="fa-solid fa-xmark"></i></button>
            </div>

            <p class="text-xs text-gray-500 dark:text-gray-400">현재 워크스페이스의 모든 페이지 본문을 합쳐서 내보냅니다.</p>

            <textarea id="export-text-area" readonly class="w-full h-48 bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-lg p-3 text-xs font-mono outline-none resize-none text-gray-800 dark:text-gray-200 select-text"></textarea>

            <div class="flex items-center justify-between pt-2">
                <button onclick="copyExportText()" class="px-3.5 py-2 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 rounded-lg text-xs font-semibold flex items-center space-x-1.5 transition">
                    <i class="fa-regular fa-copy"></i>
                    <span>클립보드 복사</span>
                </button>
                <div class="flex items-center space-x-2">
                    <button onclick="downloadAsTextFile()" class="px-4 py-2 bg-brand-600 hover:bg-brand-700 text-white rounded-lg text-xs font-bold shadow-md transition flex items-center space-x-1.5">
                        <i class="fa-solid fa-download"></i>
                        <span>.txt 파일 다운로드</span>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- TOAST NOTIFICATION CONTAINER -->
    <div id="toast-container" class="fixed top-16 right-4 z-50 space-y-2 pointer-events-none"></div>

    <!-- JAVASCRIPT APPLICATION CORE -->
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";
        import { getAuth, signInWithEmailAndPassword, createUserWithEmailAndPassword, signOut, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-auth.js";
        import { getFirestore, doc, setDoc, getDoc, collection, onSnapshot, deleteDoc } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js";

        // APP INITIAL STATE & DEFAULTS
        const appId = typeof __app_id !== 'undefined' ? __app_id : 'bookcraft-studio-app';
        let firebaseApp, db, auth;
        let currentUser = null;
        let unsubscribeWorkspaces = null;

        // Default Presets Dimensions (mm)
        const BOOK_PRESETS = {
            'sin-guk': { name: '신국판', width: 152, height: 225 },
            'a5': { name: 'A5', width: 148, height: 210 },
            'b6': { name: 'B6 / 사륙판', width: 128, height: 182 },
            'pocket': { name: '문고판', width: 105, height: 148 },
            'custom': { name: '사용자 설정', width: 152, height: 225 }
        };

        // Main Application State
        window.state = {
            activeWorkspaceId: 'ws_default',
            workspaces: {
                'ws_default': {
                    id: 'ws_default',
                    title: '나의 첫 번째 소설 원고',
                    activePageIndex: 0,
                    activePairIndex: 0,
                    book: {
                        presetKey: 'sin-guk',
                        widthMM: 152,
                        heightMM: 225,
                        isSpread: false,
                        showMarginGuide: true,
                        margins: { top: 20, bottom: 22, left: 18, right: 18 }
                    },
                    typography: {
                        fontFamily: "'Gowun Batang', serif",
                        fontSizePt: 10.5,
                        lineHeight: 1.75,
                        letterSpacingPx: 0,
                        textIndentEm: 1.0,
                        paragraphSpacingPx: 0,
                        textColor: '#111827',
                        paperColor: '#fdfbf7'
                    },
                    pages: [
                        { id: 'p1', title: '페이지 1', content: '여기에 소설의 첫 문장을 시작해 보세요.\n\n바람이 서늘하게 불어오는 가을밤, 서재 창문 너머로 오래된 도서관의 불빛이 아른거렸다. 그는 책장의 먼지를 털어내며 오랫동안 잊고 있었던 양장본 노트를 꺼내 들었다.' },
                        { id: 'p2', title: '페이지 2', content: '두 번째 페이지 본문 내용입니다. 짝수 페이지는 안쪽(제본) 여백과 바깥쪽 여백이 반전 적용됩니다. 조판 설정을 변경하면 실시간으로 정밀하게 반영됩니다.' },
                        { id: 'p3', title: '페이지 3', content: '세 번째 페이지입니다. 하단 썸네일을 700ms 동안 길게 누르면 간편하게 삭제할 수 있습니다.' }
                    ],
                    mindmap: {
                        nodes: [
                            { id: 'n1', type: 'character', title: '주인공 (한설)', desc: '기억을 잃은 고서적 복원가', x: 120, y: 140 },
                            { id: 'n2', type: 'event', title: '비밀 양장본 발견', desc: '100년 전 봉인된 문양의 노트', x: 440, y: 180 }
                        ],
                        connections: [
                            { from: 'n1', to: 'n2' }
                        ]
                    }
                }
            },
            scale: 1.0,
            activeTab: 'studio',
            
            // Mind Map Interactive Engine States
            mindmapMode: 'pan',
            mindmapPanX: 0,
            mindmapPanY: 0,
            mindmapZoom: 1.0,
            selectedConnectSourceId: null
        };

        // Initialize Firebase
        try {
            const firebaseConfig = typeof __firebase_config !== 'undefined' ? JSON.parse(__firebase_config) : null;
            if (firebaseConfig) {
                firebaseApp = initializeApp(firebaseConfig);
                auth = getAuth(firebaseApp);
                db = getFirestore(firebaseApp);

                onAuthStateChanged(auth, async (user) => {
                    currentUser = user;
                    updateAuthUI();
                    if (user) {
                        loadCloudDataForUser(user.uid);
                    } else {
                        if (unsubscribeWorkspaces) {
                            unsubscribeWorkspaces();
                            unsubscribeWorkspaces = null;
                        }
                        loadLocalStorageData();
                        renderWorkspaceList();
                        renderSpreadEditor();
                        renderMindMap();
                    }
                });
            }
        } catch (e) {
            console.warn("Firebase fallback to localStorage", e);
        }

        // INITIALIZATION
        window.addEventListener('DOMContentLoaded', () => {
            loadLocalStorageData();
            renderWorkspaceList();
            applyStateToUI();
            renderSpreadEditor();
            initMindMapEvents();
            renderMindMap();
            setupAutoSaveTimer();
        });

        function currentWS() {
            return window.state.workspaces[window.state.activeWorkspaceId] || Object.values(window.state.workspaces)[0];
        }

        /* ------------------------------------------------------------------ */
        /* EMAIL AUTHENTICATION ENGINE (SIGN UP, LOGIN, LOGOUT)               */
        /* ------------------------------------------------------------------ */
        window.handleEmailLogin = async function() {
            if (!auth) {
                showToast("인증 서비스가 연결되지 않았습니다.", "error");
                return;
            }
            const email = document.getElementById('auth-email-input').value.trim();
            const password = document.getElementById('auth-password-input').value.trim();
            if (!email || !password) {
                showToast("이메일과 비밀번호를 모두 입력하세요.", "warning");
                return;
            }
            try {
                await signInWithEmailAndPassword(auth, email, password);
                closeAuthModal();
                showToast("성공적으로 로그인되었습니다.", "success");
            } catch (error) {
                showToast(`로그인 실패: ${error.message}`, "error");
            }
        };

        window.handleEmailRegister = async function() {
            if (!auth) {
                showToast("인증 서비스가 연결되지 않았습니다.", "error");
                return;
            }
            const email = document.getElementById('auth-email-input').value.trim();
            const password = document.getElementById('auth-password-input').value.trim();
            if (!email || password.length < 6) {
                showToast("유효한 이메일과 6자리 이상 비밀번호를 입력하세요.", "warning");
                return;
            }
            try {
                await createUserWithEmailAndPassword(auth, email, password);
                closeAuthModal();
                showToast("회원가입 및 로그인이 완료되었습니다.", "success");
            } catch (error) {
                showToast(`회원가입 실패: ${error.message}`, "error");
            }
        };

        window.handleLogout = async function() {
            if (!auth) return;
            try {
                await signOut(auth);
                closeAuthModal();
                showToast("로그아웃되었습니다.", "info");
            } catch (error) {
                showToast("로그아웃 중 오류가 발생했습니다.", "error");
            }
        };

        function updateAuthUI() {
            const textEl = document.getElementById('auth-btn-text');
            const loggedInPanel = document.getElementById('logged-in-panel');
            const loggedOutPanel = document.getElementById('logged-out-panel');
            const emailDisplay = document.getElementById('current-user-email-display');
            const modalTitle = document.getElementById('auth-modal-title');
            const modalDesc = document.getElementById('auth-modal-desc');

            if (currentUser && !currentUser.isAnonymous) {
                textEl.textContent = currentUser.email;
                if (loggedInPanel) loggedInPanel.classList.remove('hidden');
                if (loggedOutPanel) loggedOutPanel.classList.add('hidden');
                if (emailDisplay) emailDisplay.textContent = `현재 계정: ${currentUser.email}`;
                if (modalTitle) modalTitle.textContent = "계정 관리";
                if (modalDesc) modalDesc.textContent = "로그인된 계정으로 안전하게 원고가 동기화되고 있습니다.";
            } else {
                textEl.textContent = "로그인 / 회원가입";
                if (loggedInPanel) loggedInPanel.classList.add('hidden');
                if (loggedOutPanel) loggedOutPanel.classList.remove('hidden');
                if (modalTitle) modalTitle.textContent = "계정 로그인 및 회원가입";
                if (modalDesc) modalDesc.textContent = "이메일 계정으로 로그인하여 내 원고를 안전하게 보관하세요.";
            }
        }

        /* ------------------------------------------------------------------ */
        /* ACCORDION FOLD/UNFOLD CONTROLLER                                   */
        /* ------------------------------------------------------------------ */
        window.toggleAccordion = function(contentId) {
            const content = document.getElementById(contentId);
            const icon = document.getElementById(`${contentId}-icon`);
            if (!content) return;

            const isHidden = content.classList.contains('hidden');
            if (isHidden) {
                content.classList.remove('hidden');
                if (icon) icon.classList.add('rotate-180');
            } else {
                content.classList.add('hidden');
                if (icon) icon.classList.remove('rotate-180');
            }
        };

        /* ------------------------------------------------------------------ */
        /* PARAGRAPH FORMATTING ENGINE                                        */
        /* ------------------------------------------------------------------ */
        window.formatTextToParagraphs = function(text) {
            if (!text) return '<p><br></p>';
            const lines = text.split(/\r?\n/);
            return lines.map(line => {
                if (line.trim() === '') {
                    return '<p><br></p>';
                }
                const escaped = line
                    .replace(/&/g, "&amp;")
                    .replace(/</g, "&lt;")
                    .replace(/>/g, "&gt;");
                return `<p>${escaped}</p>`;
            }).join('');
        };

        window.pageHTMLCache = {};
        window.handleFocus = function(idx) {
            const area = document.getElementById(`editor-page-${idx}`);
            if (area) {
                window.pageHTMLCache[idx] = area.innerHTML;
            }
        };

        window.handleInput = function(idx) {
            const ws = currentWS();
            const area = document.getElementById(`editor-page-${idx}`);
            if (!area) return;

            if (area.scrollHeight > area.clientHeight + 4) {
                area.innerHTML = window.pageHTMLCache[idx] || area.innerHTML;
                showToast("페이지 분량을 초과하여 다음 페이지로 이동합니다.", "warning");
                setTimeout(() => {
                    window.autoAdvancePage(idx);
                }, 50);
                return;
            }

            window.pageHTMLCache[idx] = area.innerHTML;
            if (ws.pages[idx]) {
                ws.pages[idx].htmlContent = area.innerHTML;
                ws.pages[idx].content = area.innerText;
            }
            updateStatistics();
        };

        window.autoAdvancePage = function(currentIdx) {
            const ws = currentWS();
            if (currentIdx === ws.pages.length - 1) {
                const newId = 'page_' + Date.now();
                ws.pages.push({
                    id: newId,
                    title: `페이지 ${ws.pages.length + 1}`,
                    content: '',
                    htmlContent: '<p><br></p>'
                });
            }
            
            if (!ws.book.isSpread) {
                ws.activePageIndex = currentIdx + 1;
            } else {
                const nextIdx = currentIdx + 1;
                const pairs = getSpreadPairs();
                const pairIdx = pairs.findIndex(p => p.leftIndex === nextIdx || p.rightIndex === nextIdx);
                if (pairIdx !== -1) {
                    ws.activePairIndex = pairIdx;
                }
            }
            
            renderSpreadEditor();
            performSaveOperation();

            setTimeout(() => {
                const nextArea = document.getElementById(`editor-page-${currentIdx + 1}`);
                if (nextArea) {
                    nextArea.focus();
                    const range = document.createRange();
                    const sel = window.getSelection();
                    range.selectNodeContents(nextArea);
                    range.collapse(true);
                    sel.removeAllRanges();
                    sel.addRange(range);
                }
            }, 50);
        };

        window.handlePagePaste = function(e, idx) {
            e.preventDefault();
            const text = (e.clipboardData || window.clipboardData).getData('text/plain');
            if (!text) return;
            const formatted = window.formatTextToParagraphs(text);
            document.execCommand('insertHTML', false, formatted);
            window.handleInput(idx);
        };

        window.toggleParagraphIndent = function() {
            const sel = window.getSelection();
            if (!sel.rangeCount) return;
            const node = sel.anchorNode;
            if (!node) return;

            let block = node.nodeType === 3 ? node.parentNode : node;
            while (block && block.nodeName !== 'P' && block.nodeName !== 'DIV' && !block.classList?.contains('book-editor-area')) {
                block = block.parentNode;
            }

            if (block && (block.nodeName === 'P' || block.nodeName === 'DIV') && !block.classList.contains('book-editor-area')) {
                block.classList.toggle('no-indent');
                
                const area = block.closest('.book-editor-area');
                if (area) {
                    const idxStr = area.id.replace('editor-page-', '');
                    const idx = parseInt(idxStr);
                    const ws = currentWS();
                    if (ws.pages[idx]) {
                        ws.pages[idx].htmlContent = area.innerHTML;
                        window.pageHTMLCache[idx] = area.innerHTML;
                        performSaveOperation();
                        showToast("선택한 문단의 들여쓰기 설정이 변경되었습니다.", "info");
                    }
                }
            }
        };

        /* ------------------------------------------------------------------ */
        /* SAVE & PERSISTENCE LOGIC (USER SPECIFIC FIRESTORE STORAGE)           */
        /* ------------------------------------------------------------------ */
        function setupAutoSaveTimer() {
            const TEN_MINUTES_MS = 10 * 60 * 1000;
            setInterval(async () => {
                await performSaveOperation();
                const nowStr = new Date().toLocaleTimeString('ko-KR', { hour: '2-digit', minute: '2-digit' });
                updateSyncBadge(`자동 저장: 10분 주기 (${nowStr})`);
                showToast("10분 주기 자동 저장이 완료되었습니다.", "info");
            }, TEN_MINUTES_MS);
        }

        window.manualSaveNow = async function() {
            await performSaveOperation();
            showToast("작업 내용이 성공적으로 저장되었습니다!", "success");
            const nowStr = new Date().toLocaleTimeString('ko-KR', { hour: '2-digit', minute: '2-digit' });
            updateSyncBadge(`지금 저장됨 (${nowStr})`);
        };

        async function performSaveOperation() {
            try {
                localStorage.setItem('bookcraft_studio_data', JSON.stringify({
                    activeWorkspaceId: window.state.activeWorkspaceId,
                    workspaces: window.state.workspaces
                }));
            } catch (e) {
                console.error("Local storage error:", e);
            }

            if (db && currentUser && !currentUser.isAnonymous) {
                try {
                    const wsDocRef = doc(db, 'artifacts', appId, 'users', currentUser.uid, 'workspaces', window.state.activeWorkspaceId);
                    await setDoc(wsDocRef, currentWS(), { merge: true });
                } catch (err) {
                    console.error("Firestore sync error:", err);
                }
            }
        }

        function updateSyncBadge(msg) {
            const el = document.getElementById('sync-status-text');
            if (el) el.textContent = msg;
        }

        /* ------------------------------------------------------------------ */
        /* WORKSPACE & PAGE MANAGEMENT                                        */
        /* ------------------------------------------------------------------ */
        window.openRenameWorkspaceModal = function() {
            const ws = currentWS();
            const input = document.getElementById('rename-ws-title-input');
            if (input) input.value = ws.title || '';
            document.getElementById('modal-rename-workspace').classList.remove('hidden');
            if (input) input.focus();
        };

        window.closeRenameWorkspaceModal = function() {
            document.getElementById('modal-rename-workspace').classList.add('hidden');
        };

        window.confirmRenameWorkspace = function() {
            const input = document.getElementById('rename-ws-title-input');
            const newTitle = input ? input.value.trim() : '';
            if (!newTitle) {
                showToast("유효한 작품 제목을 입력하세요.", "warning");
                return;
            }

            const ws = currentWS();
            ws.title = newTitle;
            closeRenameWorkspaceModal();
            renderWorkspaceList();
            renderSpreadEditor();
            performSaveOperation();
            showToast(`원고 제목이 "${newTitle}"(으)로 변경되었습니다.`, "success");
        };

        let targetDeletePageIndex = null;
        let targetDeletePair = null;

        window.openDeletePageModal = function(indexOrPair, isSpread) {
            const modal = document.getElementById('modal-delete-page');
            const textEl = document.getElementById('delete-page-modal-text');
            const confirmBtn = document.getElementById('confirm-delete-page-btn');

            if (!isSpread) {
                targetDeletePageIndex = indexOrPair;
                targetDeletePair = null;
                const pageNum = indexOrPair + 1;
                textEl.innerHTML = `<strong>Page ${pageNum}</strong> 페이지를 삭제합니다. 이 작업은 되돌릴 수 없습니다.`;
                confirmBtn.onclick = () => {
                    executeDeleteSinglePage(targetDeletePageIndex);
                    closeDeletePageModal();
                };
            } else {
                targetDeletePair = indexOrPair;
                targetDeletePageIndex = null;
                const p1 = indexOrPair.leftIndex + 1;
                const p2 = indexOrPair.rightIndex !== null ? indexOrPair.rightIndex + 1 : null;
                const pStr = p2 ? `Page ${p1} & Page ${p2}` : `Page ${p1}`;
                textEl.innerHTML = `<strong>${pStr}</strong> (펼침면)의 페이지를 삭제합니다.`;
                confirmBtn.onclick = () => {
                    executeDeleteSpreadPair(targetDeletePair);
                    closeDeletePageModal();
                };
            }

            modal.classList.remove('hidden');
        };

        window.closeDeletePageModal = function() {
            document.getElementById('modal-delete-page').classList.add('hidden');
        };

        function executeDeleteSinglePage(index) {
            const ws = currentWS();
            if (ws.pages.length <= 1) {
                showToast("최소 1개의 페이지가 유지되어야 합니다.", "warning");
                return;
            }
            ws.pages.splice(index, 1);
            if ((ws.activePageIndex || 0) >= ws.pages.length) {
                ws.activePageIndex = Math.max(0, ws.pages.length - 1);
            }
            renderSpreadEditor();
            performSaveOperation();
            showToast("페이지가 삭제되었습니다.", "error");
        }

        function executeDeleteSpreadPair(pair) {
            const ws = currentWS();
            if (ws.pages.length <= 1) {
                showToast("최소 1개의 페이지가 유지되어야 합니다.", "warning");
                return;
            }
            if (pair.rightIndex !== null) ws.pages.splice(pair.rightIndex, 1);
            if (pair.leftIndex !== null) ws.pages.splice(pair.leftIndex, 1);

            const pairs = getSpreadPairs();
            if ((ws.activePairIndex || 0) >= pairs.length) {
                ws.activePairIndex = Math.max(0, pairs.length - 1);
            }
            renderSpreadEditor();
            performSaveOperation();
            showToast("선택된 펼침면 페이지가 삭제되었습니다.", "error");
        }

        window.jumpToPage = function() {
            const input = document.getElementById('jump-page-input');
            let targetPage = parseInt(input.value);
            const ws = currentWS();

            if (isNaN(targetPage) || targetPage < 1) {
                showToast("유효한 페이지 번호를 입력하세요.", "warning");
                return;
            }

            if (targetPage > ws.pages.length) {
                targetPage = ws.pages.length;
            }

            const targetIndex = targetPage - 1;

            if (!ws.book.isSpread) {
                ws.activePageIndex = targetIndex;
            } else {
                const pairs = getSpreadPairs();
                const pairIdx = pairs.findIndex(p => p.leftIndex === targetIndex || p.rightIndex === targetIndex);
                if (pairIdx !== -1) {
                    ws.activePairIndex = pairIdx;
                }
            }

            renderSpreadEditor();
            input.value = '';
            showToast(`${targetPage} 페이지로 이동했습니다.`, "info");
        };

        function getSpreadPairs() {
            const ws = currentWS();
            const pairs = [];
            for (let i = 0; i < ws.pages.length; i += 2) {
                pairs.push({
                    leftIndex: i,
                    rightIndex: (i + 1 < ws.pages.length) ? i + 1 : null
                });
            }
            return pairs;
        }

        window.setSpreadMode = function(isSpread) {
            const ws = currentWS();
            ws.book.isSpread = isSpread;

            document.getElementById('btn-mode-single').className = !isSpread 
                ? 'p-2 border rounded-lg font-semibold flex flex-col items-center space-y-1 transition border-brand-500 bg-brand-50 dark:bg-brand-900/30 text-brand-600 dark:text-brand-300'
                : 'p-2 border rounded-lg font-semibold flex flex-col items-center space-y-1 transition border-gray-200 dark:border-gray-600 text-gray-500';

            document.getElementById('btn-mode-spread').className = isSpread 
                ? 'p-2 border rounded-lg font-semibold flex flex-col items-center space-y-1 transition border-brand-500 bg-brand-50 dark:bg-brand-900/30 text-brand-600 dark:text-brand-300'
                : 'p-2 border rounded-lg font-semibold flex flex-col items-center space-y-1 transition border-gray-200 dark:border-gray-600 text-gray-500';

            renderSpreadEditor();
            performSaveOperation();
        };

        function generatePageCSS(pageIndex) {
            const ws = currentWS();
            const mmToPx = 3.7795275591;
            const widthPx = ws.book.widthMM * mmToPx;
            const heightPx = ws.book.heightMM * mmToPx;
            const font = ws.typography;
            const baseMargins = ws.book.margins;

            const isEvenPage = (pageIndex % 2 === 1);
            
            const effectiveLeftMargin = isEvenPage ? baseMargins.right : baseMargins.left;
            const effectiveRightMargin = isEvenPage ? baseMargins.left : baseMargins.right;

            const topPx = baseMargins.top * mmToPx;
            const bottomPx = baseMargins.bottom * mmToPx;
            const leftPx = effectiveLeftMargin * mmToPx;
            const rightPx = effectiveRightMargin * mmToPx;

            const pageStyle = `
                width: ${widthPx}px;
                height: ${heightPx}px;
                background-color: ${font.paperColor};
                color: ${font.textColor};
                font-family: ${font.fontFamily};
                font-size: ${font.fontSizePt}pt;
                line-height: ${font.lineHeight};
                letter-spacing: ${font.letterSpacingPx}px;
            `;

            const guideBoxStyle = `top:${topPx}px; bottom:${bottomPx}px; left:${leftPx}px; right:${rightPx}px;`;
            const editorBoxStyle = `top:${topPx}px; bottom:${bottomPx}px; left:${leftPx}px; right:${rightPx}px;`;
            const headerStyle = `left:${leftPx}px; right:${rightPx}px; top:${Math.max(6, topPx / 2 - 8)}px;`;
            const footerStyle = `left:${leftPx}px; right:${rightPx}px; bottom:${Math.max(6, bottomPx / 2 - 8)}px;`;

            return { pageStyle, guideBoxStyle, editorBoxStyle, headerStyle, footerStyle, widthPx, heightPx, mmToPx };
        }

        function renderSpreadEditor() {
            const ws = currentWS();
            const container = document.getElementById('book-sheet-container');
            const scale = window.state.scale;
            container.style.transform = `scale(${scale})`;

            const font = ws.typography;
            const textIndentEm = font.textIndentEm !== undefined ? font.textIndentEm : 1.0;
            const paragraphSpacingPx = font.paragraphSpacingPx !== undefined ? font.paragraphSpacingPx : 0;

            container.style.setProperty('--text-indent', `${textIndentEm}em`);
            container.style.setProperty('--para-spacing', `${paragraphSpacingPx}px`);

            let html = '';

            if (!ws.book.isSpread) {
                const idx = ws.activePageIndex || 0;
                const page = ws.pages[idx] || ws.pages[0];
                const pageHtml = page ? (page.htmlContent || window.formatTextToParagraphs(page.content)) : '';
                const { pageStyle, guideBoxStyle, editorBoxStyle, headerStyle, footerStyle } = generatePageCSS(idx);

                html += `
                    <div class="relative shadow-2xl rounded-sm transition-all overflow-hidden" style="${pageStyle}">
                        ${ws.book.showMarginGuide ? `<div class="margin-guide-box absolute inset-0" style="${guideBoxStyle}"></div>` : ''}
                        
                        <div class="absolute flex items-center justify-between text-[10px] opacity-40 select-none font-sans z-10" style="${headerStyle}">
                            <span>${ws.title}</span>
                            <span>Page ${idx + 1}</span>
                        </div>

                        <div id="editor-page-${idx}" contenteditable="true" onfocus="handleFocus(${idx})" oninput="handleInput(${idx})" onpaste="handlePagePaste(event, ${idx})" class="book-editor-area absolute outline-none overflow-hidden z-10 py-1" style="${editorBoxStyle}; word-break: break-all;">${pageHtml}</div>

                        <div class="absolute text-center text-[10px] opacity-50 font-sans z-10" style="${footerStyle}">
                            - ${idx + 1} -
                        </div>
                    </div>
                `;
            } else {
                const pairs = getSpreadPairs();
                if ((ws.activePairIndex || 0) >= pairs.length) {
                    ws.activePairIndex = Math.max(0, pairs.length - 1);
                }
                const pair = pairs[ws.activePairIndex || 0];
                const leftPage = ws.pages[pair.leftIndex];
                const rightPage = pair.rightIndex !== null ? ws.pages[pair.rightIndex] : null;

                const leftHtml = leftPage ? (leftPage.htmlContent || window.formatTextToParagraphs(leftPage.content)) : '';
                const rightHtml = rightPage ? (rightPage.htmlContent || window.formatTextToParagraphs(rightPage.content)) : '';

                const leftCSS = generatePageCSS(pair.leftIndex);
                const rightCSS = rightPage ? generatePageCSS(pair.rightIndex) : null;

                html += `
                    <div class="flex shadow-2xl rounded-sm overflow-hidden border border-gray-300/40">
                        <div class="relative spine-crease-left border-r border-gray-300/50" style="${leftCSS.pageStyle}">
                            ${ws.book.showMarginGuide ? `<div class="margin-guide-box absolute inset-0" style="${leftCSS.guideBoxStyle}"></div>` : ''}
                            
                            <div class="absolute flex items-center justify-between text-[10px] opacity-40 select-none font-sans z-10" style="${leftCSS.headerStyle}">
                                <span>${ws.title}</span>
                                <span>Page ${pair.leftIndex + 1}</span>
                            </div>

                            <div id="editor-page-${pair.leftIndex}" contenteditable="true" onfocus="handleFocus(${pair.leftIndex})" oninput="handleInput(${pair.leftIndex})" onpaste="handlePagePaste(event, ${pair.leftIndex})" class="book-editor-area absolute outline-none overflow-hidden z-10 py-1" style="${leftCSS.editorBoxStyle}; word-break: break-all;">${leftHtml}</div>

                            <div class="absolute text-center text-[10px] opacity-50 font-sans z-10" style="${leftCSS.footerStyle}">
                                - ${pair.leftIndex + 1} -
                            </div>
                        </div>

                        ${rightPage ? `
                            <div class="relative spine-crease-right" style="${rightCSS.pageStyle}">
                                ${ws.book.showMarginGuide ? `<div class="margin-guide-box absolute inset-0" style="${rightCSS.guideBoxStyle}"></div>` : ''}
                                
                                <div class="absolute flex items-center justify-between text-[10px] opacity-40 select-none font-sans z-10" style="${rightCSS.headerStyle}">
                                    <span>Page ${pair.rightIndex + 1}</span>
                                    <span>${ws.title}</span>
                                </div>

                                <div id="editor-page-${pair.rightIndex}" contenteditable="true" onfocus="handleFocus(${pair.rightIndex})" oninput="handleInput(${pair.rightIndex})" onpaste="handlePagePaste(event, ${pair.rightIndex})" class="book-editor-area absolute outline-none overflow-hidden z-10 py-1" style="${rightCSS.editorBoxStyle}; word-break: break-all;">${rightHtml}</div>

                                <div class="absolute text-center text-[10px] opacity-50 font-sans z-10" style="${rightCSS.footerStyle}">
                                    - ${pair.rightIndex + 1} -
                                </div>
                            </div>
                        ` : `
                            <div class="relative bg-gray-100 dark:bg-gray-800 flex items-center justify-center opacity-40" style="width:${leftCSS.widthPx}px; height:${leftCSS.heightPx}px;">
                                <span class="text-xs font-sans text-gray-500">페이지 없음 (홀수 완결)</span>
                            </div>
                        `}
                    </div>
                `;
            }

            container.innerHTML = html;
            
            try {
                document.execCommand('defaultParagraphSeparator', false, 'p');
            } catch(e) {}

            renderPageThumbnails();
            updateHeaderPageInfo();
            updateStatistics();
        }

        function renderPageThumbnails() {
            const ws = currentWS();
            const strip = document.getElementById('page-thumbnails-strip');
            strip.innerHTML = '';

            const attachLongPress = (btn, onLongPress, onClick) => {
                let pressTimer = null;
                let isLongPress = false;

                const startPress = (e) => {
                    if (e.type === 'mousedown' && e.button !== 0) return;
                    isLongPress = false;
                    btn.classList.add('pressing-active');
                    pressTimer = setTimeout(() => {
                        isLongPress = true;
                        btn.classList.remove('pressing-active');
                        if (navigator.vibrate) navigator.vibrate(50);
                        onLongPress();
                    }, 700);
                };

                const cancelPress = () => {
                    btn.classList.remove('pressing-active');
                    clearTimeout(pressTimer);
                };

                btn.addEventListener('mousedown', startPress);
                btn.addEventListener('touchstart', startPress, { passive: true });
                btn.addEventListener('mouseup', cancelPress);
                btn.addEventListener('mouseleave', cancelPress);
                btn.addEventListener('touchend', cancelPress);
                btn.addEventListener('touchcancel', cancelPress);
                btn.addEventListener('contextmenu', (e) => e.preventDefault());

                btn.onclick = (e) => {
                    cancelPress();
                    if (isLongPress) {
                        e.preventDefault();
                        return;
                    }
                    onClick();
                };
            };

            if (!ws.book.isSpread) {
                ws.pages.forEach((p, index) => {
                    const btn = document.createElement('button');
                    btn.style.cssText = "-webkit-touch-callout: none;";
                    const isActive = index === (ws.activePageIndex || 0);
                    btn.className = `px-3 py-1 rounded-md text-xs font-semibold shrink-0 transition flex items-center space-x-1 ${
                        isActive 
                            ? 'bg-brand-600 text-white shadow-sm ring-2 ring-brand-400' 
                            : 'bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300'
                    }`;
                    btn.innerHTML = `<span>Page ${index + 1}</span>`;

                    attachLongPress(
                        btn,
                        () => openDeletePageModal(index, false),
                        () => {
                            ws.activePageIndex = index;
                            renderSpreadEditor();
                        }
                    );

                    strip.appendChild(btn);
                });
            } else {
                const pairs = getSpreadPairs();
                pairs.forEach((pair, pairIdx) => {
                    const btn = document.createElement('button');
                    btn.style.cssText = "-webkit-touch-callout: none;";
                    const isActive = pairIdx === (ws.activePairIndex || 0);
                    const p1 = pair.leftIndex + 1;
                    const p2 = pair.rightIndex !== null ? pair.rightIndex + 1 : '-';

                    btn.className = `px-3 py-1 rounded-md text-xs font-semibold shrink-0 transition flex items-center space-x-1 ${
                        isActive 
                            ? 'bg-brand-600 text-white shadow-sm ring-2 ring-brand-400' 
                            : 'bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300'
                    }`;
                    btn.innerHTML = `<i class="fa-solid fa-book-open text-[10px] opacity-70 mr-1"></i><span>P.${p1}-${p2}</span>`;

                    attachLongPress(
                        btn,
                        () => openDeletePageModal(pair, true),
                        () => {
                            ws.activePairIndex = pairIdx;
                            renderSpreadEditor();
                        }
                    );

                    strip.appendChild(btn);
                });
            }
        }

        window.addNewPage = function() {
            const ws = currentWS();
            const newId = 'page_' + Date.now();
            ws.pages.push({
                id: newId,
                title: `페이지 ${ws.pages.length + 1}`,
                content: '',
                htmlContent: '<p><br></p>'
            });

            if (!ws.book.isSpread) {
                ws.activePageIndex = ws.pages.length - 1;
            } else {
                const pairs = getSpreadPairs();
                ws.activePairIndex = pairs.length - 1;
            }

            renderSpreadEditor();
            performSaveOperation();
            showToast("새 원고 페이지가 추가되었습니다.", "success");
        };

        function updateHeaderPageInfo() {
            const ws = currentWS();
            const info = document.getElementById('canvas-page-info');
            const spreadBadge = document.getElementById('info-spread-badge');

            if (!ws.book.isSpread) {
                info.textContent = `Page ${(ws.activePageIndex || 0) + 1} / ${ws.pages.length}`;
                spreadBadge.textContent = "단면 편집 모드";
            } else {
                const pairs = getSpreadPairs();
                const pair = pairs[ws.activePairIndex || 0];
                if (pair) {
                    const p2 = pair.rightIndex !== null ? pair.rightIndex + 1 : '';
                    info.textContent = `Pages ${pair.leftIndex + 1}${p2 ? ' - ' + p2 : ''} / ${ws.pages.length}`;
                }
                spreadBadge.textContent = "양면 펼침면 모드";
            }
        }

        window.applyBookPreset = function(presetKey) {
            const ws = currentWS();
            ws.book.presetKey = presetKey;
            const preset = BOOK_PRESETS[presetKey];
            const customBox = document.getElementById('custom-size-box');

            if (presetKey === 'custom') {
                customBox.classList.remove('hidden');
            } else {
                customBox.classList.add('hidden');
                ws.book.widthMM = preset.width;
                ws.book.heightMM = preset.height;
                document.getElementById('setting-width').value = preset.width;
                document.getElementById('setting-height').value = preset.height;
            }

            document.getElementById('info-dimension-badge').textContent = `${preset.name} (${ws.book.widthMM} × ${ws.book.heightMM} mm)`;
            renderSpreadEditor();
            performSaveOperation();
        };

        window.updateCustomSize = function() {
            const ws = currentWS();
            ws.book.widthMM = parseFloat(document.getElementById('setting-width').value) || 152;
            ws.book.heightMM = parseFloat(document.getElementById('setting-height').value) || 225;
            document.getElementById('info-dimension-badge').textContent = `사용자 지정 (${ws.book.widthMM} × ${ws.book.heightMM} mm)`;
            renderSpreadEditor();
            performSaveOperation();
        };

        window.toggleMarginGuide = function(checked) {
            currentWS().book.showMarginGuide = checked;
            renderSpreadEditor();
        };

        window.updateTypographySettings = function() {
            const ws = currentWS();
            ws.typography.fontFamily = document.getElementById('setting-font-family').value;
            ws.typography.fontSizePt = parseFloat(document.getElementById('setting-font-size').value);
            ws.typography.lineHeight = parseFloat(document.getElementById('setting-line-height').value);
            ws.typography.letterSpacingPx = parseFloat(document.getElementById('setting-letter-spacing').value);
            ws.typography.textIndentEm = parseFloat(document.getElementById('setting-text-indent').value);
            ws.typography.paragraphSpacingPx = parseFloat(document.getElementById('setting-paragraph-spacing').value);
            ws.typography.textColor = document.getElementById('setting-text-color').value;
            ws.typography.paperColor = document.getElementById('setting-paper-color').value;

            ws.book.margins.top = parseFloat(document.getElementById('setting-margin-top').value) || 20;
            ws.book.margins.bottom = parseFloat(document.getElementById('setting-margin-bottom').value) || 22;
            ws.book.margins.left = parseFloat(document.getElementById('setting-margin-left').value) || 18;
            ws.book.margins.right = parseFloat(document.getElementById('setting-margin-right').value) || 18;

            document.getElementById('label-font-size').textContent = `${ws.typography.fontSizePt}pt`;
            const lhVal = ws.typography.lineHeight;
            document.getElementById('label-line-height').textContent = lhVal === 1.0 ? '1.0 (없음)' : lhVal;
            document.getElementById('label-letter-spacing').textContent = `${ws.typography.letterSpacingPx}px`;
            document.getElementById('label-text-indent').textContent = `${ws.typography.textIndentEm}em`;
            document.getElementById('label-paragraph-spacing').textContent = `${ws.typography.paragraphSpacingPx}px`;
            document.getElementById('label-text-color').textContent = ws.typography.textColor;

            renderSpreadEditor();
            performSaveOperation();
        };

        window.setPaperColor = function(colorHex) {
            const ws = currentWS();
            ws.typography.paperColor = colorHex;
            document.getElementById('setting-paper-color').value = colorHex;
            renderSpreadEditor();
            performSaveOperation();
        };

        window.resetBookSettings = function() {
            const ws = currentWS();
            ws.book.margins = { top: 20, bottom: 22, left: 18, right: 18 };
            ws.typography = {
                fontFamily: "'Gowun Batang', serif",
                fontSizePt: 10.5,
                lineHeight: 1.75,
                letterSpacingPx: 0,
                textIndentEm: 1.0,
                paragraphSpacingPx: 0,
                textColor: '#111827',
                paperColor: '#fdfbf7'
            };
            applyStateToUI();
            renderSpreadEditor();
            showToast("설정이 초기화되었습니다.", "info");
        };

        function updateStatistics() {
            const ws = currentWS();
            let totalChars = 0;
            let totalWords = 0;

            ws.pages.forEach(p => {
                const textForStats = p.content || '';
                totalChars += textForStats.length;
                totalWords += textForStats.trim() ? textForStats.trim().split(/\s+/).length : 0;
            });

            const ptToPx = 1.333333;
            const mmToPx = 3.7795275591;

            const printableWidthMM = Math.max(10, ws.book.widthMM - (ws.book.margins.left + ws.book.margins.right));
            const printableHeightPx = (ws.book.heightMM - ws.book.margins.top - ws.book.margins.bottom) * mmToPx;

            const UI_OVERHEAD_PX = 61;
            const availableHeightPx = Math.max(0, printableHeightPx - UI_OVERHEAD_PX);

            const fontPt = ws.typography.fontSizePt;
            const fontSizePx = fontPt * ptToPx;
            const lineHeightPx = fontSizePx * Math.max(1.0, ws.typography.lineHeight);

            const estLinesPerPage = Math.floor(availableHeightPx / lineHeightPx);

            const charWidthPx = fontSizePx + (ws.typography.letterSpacingPx || 0);
            const printableWidthPx = printableWidthMM * mmToPx;
            const estCharsPerLine = Math.floor(printableWidthPx / charWidthPx);

            const estCharsPerPage = estCharsPerLine * Math.max(0, estLinesPerPage);
            const manuscriptPages = (totalChars / 200).toFixed(1);

            document.getElementById('stat-chars-per-line').textContent = `약 ${estCharsPerLine} 자`;
            document.getElementById('stat-lines-per-page').textContent = `약 ${estLinesPerPage} 줄`;
            document.getElementById('stat-chars-per-page').textContent = `약 ${estCharsPerPage} 자`;
            document.getElementById('stat-manuscript-pages').textContent = `${manuscriptPages} 매`;
            document.getElementById('stat-total-chars').textContent = totalChars.toLocaleString();
            document.getElementById('stat-total-words').textContent = totalWords.toLocaleString();
        }

        window.openExportModal = function() {
            try {
                if (typeof saveCurrentPairContent === 'function') {
                    saveCurrentPairContent();
                }
            } catch (e) {
                console.warn("현재 페이지 저장 중 문제가 발생했으나 내보내기를 계속 진행합니다.", e);
            }

            const ws = currentWS();
            if (!ws || !ws.pages) {
                showToast("원고 데이터를 불러올 수 없습니다.", "error");
                return;
            }

            const extractText = (content) => {
                if (!content) return '';
                const tmp = document.createElement("div");
                tmp.innerHTML = content;
                return tmp.innerText || tmp.textContent || '';
            };

            const fullText = ws.pages.map((p, idx) => {
                const cleanText = extractText(p?.htmlContent || p?.content || '').trim();
                return `[ ${idx + 1} 페이지 ]\n\n${cleanText}`;
            }).join('\n\n----------------------------------------\n\n');

            const textArea = document.getElementById('export-text-area');
            if (textArea) {
                textArea.value = fullText;
            }
            
            const modal = document.getElementById('modal-export');
            if (modal) {
                modal.classList.remove('hidden');
            } else {
                showToast("내보내기 창을 찾을 수 없습니다.", "error");
            }
        };

        window.closeExportModal = function() {
            document.getElementById('modal-export').classList.add('hidden');
        };

        window.copyExportText = async function() {
            const textArea = document.getElementById('export-text-area');
            if (!textArea || !textArea.value) return;
            
            try {
                if (navigator.clipboard && window.isSecureContext) {
                    await navigator.clipboard.writeText(textArea.value);
                    showToast("원고가 클립보드에 복사되었습니다.", "success");
                } else {
                    textArea.select();
                    document.execCommand('copy');
                    window.getSelection().removeAllRanges();
                    showToast("원고가 클립보드에 복사되었습니다.", "success");
                }
            } catch (err) {
                console.error("복사 실패:", err);
                showToast("클립보드 복사를 지원하지 않는 환경입니다.", "error");
            }
        };

        window.downloadAsTextFile = function() {
            const ws = currentWS();
            const textArea = document.getElementById('export-text-area');
            const text = textArea ? textArea.value : '';
            
            if (!text.trim()) {
                showToast("내보낼 원고 내용이 없습니다.", "warning");
                return;
            }

            try {
                const blob = new Blob([text], { type: 'text/plain;charset=utf-8' });
                const url = URL.createObjectURL(blob);
                const a = document.createElement('a');
                a.style.display = 'none';
                a.href = url;
                
                const safeTitle = (ws.title || '소설_원고').replace(/[\\\/\:\*\?\"\<\>\|]/g, '_');
                a.download = `${safeTitle}.txt`;
                
                document.body.appendChild(a);
                a.click();
                
                setTimeout(() => {
                    document.body.removeChild(a);
                    URL.revokeObjectURL(url);
                }, 100);
                
                showToast("원고 파일(.txt)이 안전하게 다운로드되었습니다.", "success");
            } catch (e) {
                console.error("다운로드 실패:", e);
                showToast("파일 다운로드 중 오류가 발생했습니다.", "error");
            }
        };


        window.adjustScale = function(delta) {
            window.state.scale = Math.max(0.5, Math.min(1.6, window.state.scale + delta));
            document.getElementById('scale-label').textContent = `${Math.round(window.state.scale * 100)}%`;
            renderSpreadEditor();
        };

        window.setScale = function(val) {
            window.state.scale = val;
            document.getElementById('scale-label').textContent = `${Math.round(val * 100)}%`;
            renderSpreadEditor();
        };

        window.toggleSidebar = function() {
            const sidebar = document.getElementById('sidebar-panel');
            const expandBtn = document.getElementById('btn-expand-sidebar');
            
            sidebar.classList.toggle('-ml-80');
            
            if (sidebar.classList.contains('-ml-80')) {
                expandBtn.classList.remove('hidden');
            } else {
                expandBtn.classList.add('hidden');
            }
        };

        window.setMindMapMode = function(mode) {
            window.state.mindmapMode = mode;
            window.state.selectedConnectSourceId = null;

            const btnPan = document.getElementById('mm-mode-pan');
            const btnNode = document.getElementById('mm-mode-node');
            const btnConnect = document.getElementById('mm-mode-connect');
            const canvasEl = document.getElementById('mindmap-canvas');
            const statusText = document.getElementById('mm-status-text');

            const previewLine = document.getElementById('svg-preview-line');
            if (previewLine) previewLine.classList.add('hidden');

            canvasEl.classList.remove('mode-pan-active', 'mode-node-active', 'mode-connect-active');

            if (mode === 'pan') {
                btnPan.className = "px-3 py-1 rounded-md transition flex items-center space-x-1.5 bg-brand-600 text-white shadow";
                btnNode.className = "px-3 py-1 rounded-md transition flex items-center space-x-1.5 text-gray-600 dark:text-gray-300";
                btnConnect.className = "px-3 py-1 rounded-md transition flex items-center space-x-1.5 text-gray-600 dark:text-gray-300";
                canvasEl.classList.add('mode-pan-active');
                canvasEl.style.cursor = 'grab';
                statusText.innerHTML = "<strong>🖐️ 캔버스 이동 모드:</strong> 바탕화면이나 노드 위를 잡고 드래그하여 화면 위치를 이동하세요. (연결선 중앙의 <strong>×</strong> 클릭 시 끊기)";
            } else if (mode === 'node') {
                btnNode.className = "px-3 py-1 rounded-md transition flex items-center space-x-1.5 bg-brand-600 text-white shadow";
                btnPan.className = "px-3 py-1 rounded-md transition flex items-center space-x-1.5 text-gray-600 dark:text-gray-300";
                btnConnect.className = "px-3 py-1 rounded-md transition flex items-center space-x-1.5 text-gray-600 dark:text-gray-300";
                canvasEl.classList.add('mode-node-active');
                canvasEl.style.cursor = 'default';
                statusText.innerHTML = "<strong>🎯 노드 이동 모드:</strong> 각 노드의 카드를 잡고 원하는 위치로 드래그하여 배치하세요.";
            } else if (mode === 'connect') {
                btnConnect.className = "px-3 py-1 rounded-md transition flex items-center space-x-1.5 bg-amber-600 text-white shadow animate-pulse";
                btnPan.className = "px-3 py-1 rounded-md transition flex items-center space-x-1.5 text-gray-600 dark:text-gray-300";
                btnNode.className = "px-3 py-1 rounded-md transition flex items-center space-x-1.5 text-gray-600 dark:text-gray-300";
                canvasEl.classList.add('mode-connect-active');
                canvasEl.style.cursor = 'crosshair';
                statusText.innerHTML = "<strong>🔗 노드 연결 모드:</strong> 출발 노드 클릭 후 대상 노드를 클릭하세요. (이미 연결된 노드를 재선택하거나 선 중앙의 <strong>×</strong> 클릭 시 끊기)";
            }

            renderMindMap();
        };

        window.zoomMindMap = function(delta) {
            window.state.mindmapZoom = Math.max(0.4, Math.min(2.5, window.state.mindmapZoom + delta));
            document.getElementById('mm-zoom-label').textContent = `${Math.round(window.state.mindmapZoom * 100)}%`;
            updateMindMapTransform();
        };

        window.resetMindMapTransform = function() {
            window.state.mindmapZoom = 1.0;
            window.state.mindmapPanX = 0;
            window.state.mindmapPanY = 0;
            document.getElementById('mm-zoom-label').textContent = '100%';
            updateMindMapTransform();
        };

        function updateMindMapTransform() {
            const transformLayer = document.getElementById('mindmap-transform-layer');
            if (transformLayer) {
                transformLayer.style.transform = `translate(${window.state.mindmapPanX}px, ${window.state.mindmapPanY}px) scale(${window.state.mindmapZoom})`;
            }
        }

        window.addMindMapNode = function(type) {
            const ws = currentWS();
            const id = 'node_' + Date.now();
            const titles = {
                character: '새 인물',
                event: '새 사건',
                plot: '새 플롯',
                clue: '새 복선',
                setting: '새 배경'
            };
            
            const centerX = (-window.state.mindmapPanX + 250) / window.state.mindmapZoom;
            const centerY = (-window.state.mindmapPanY + 180) / window.state.mindmapZoom;

            ws.mindmap.nodes.push({
                id,
                type,
                title: titles[type] || '노드',
                desc: '설명을 입력하세요',
                x: Math.max(50, centerX + (Math.random() * 60 - 30)),
                y: Math.max(50, centerY + (Math.random() * 60 - 30))
            });

            window.setMindMapMode('node');
            renderMindMap();
            performSaveOperation();
            showToast("새 노드가 마인드맵에 추가되었습니다.", "success");
        };

        window.clearMindMap = function() {
            const ws = currentWS();
            ws.mindmap.nodes = [];
            ws.mindmap.connections = [];
            renderMindMap();
            performSaveOperation();
            showToast("마인드맵이 초기화되었습니다.", "info");
        };

        function initMindMapEvents() {
            const canvasEl = document.getElementById('mindmap-canvas');
            if (!canvasEl) return;

            let isPanDragging = false;
            let startPointerX = 0, startPointerY = 0;
            let startPanX = 0, startPanY = 0;

            canvasEl.onpointerdown = (e) => {
                if (e.target.getAttribute('contenteditable') === 'true') return;
                if (e.target.closest('.connection-group')) return;

                if (window.state.mindmapMode === 'pan' || e.button === 1) {
                    isPanDragging = true;
                    startPointerX = e.clientX;
                    startPointerY = e.clientY;
                    startPanX = window.state.mindmapPanX;
                    startPanY = window.state.mindmapPanY;
                    canvasEl.setPointerCapture(e.pointerId);
                    canvasEl.style.cursor = 'grabbing';
                }
            };

            canvasEl.onpointermove = (e) => {
                if (isPanDragging) {
                    window.state.mindmapPanX = startPanX + (e.clientX - startPointerX);
                    window.state.mindmapPanY = startPanY + (e.clientY - startPointerY);
                    updateMindMapTransform();
                } else if (window.state.mindmapMode === 'connect' && window.state.selectedConnectSourceId) {
                    const sourceNode = currentWS().mindmap.nodes.find(n => n.id === window.state.selectedConnectSourceId);
                    if (sourceNode) {
                        const previewLine = document.getElementById('svg-preview-line');
                        const transformLayer = document.getElementById('mindmap-transform-layer');
                        const rect = transformLayer.getBoundingClientRect();
                        
                        const sourceX = sourceNode.x + 90;
                        const sourceY = sourceNode.y + 40;
                        const targetX = (e.clientX - rect.left) / window.state.mindmapZoom;
                        const targetY = (e.clientY - rect.top) / window.state.mindmapZoom;

                        previewLine.setAttribute('x1', sourceX);
                        previewLine.setAttribute('y1', sourceY);
                        previewLine.setAttribute('x2', targetX);
                        previewLine.setAttribute('y2', targetY);
                        previewLine.classList.remove('hidden');
                    }
                }
            };

            canvasEl.onpointerup = canvasEl.onpointercancel = (e) => {
                if (isPanDragging) {
                    isPanDragging = false;
                    try { canvasEl.releasePointerCapture(e.pointerId); } catch(err) {}
                    if (window.state.mindmapMode === 'pan') {
                        canvasEl.style.cursor = 'grab';
                    }
                }
            };

            canvasEl.onwheel = (e) => {
                e.preventDefault();
                const zoomFactor = e.deltaY < 0 ? 0.08 : -0.08;
                window.zoomMindMap(zoomFactor);
            };
        }

        let activeNodeDrag = null;

        function renderMindMap() {
            const ws = currentWS();
            const nodesContainer = document.getElementById('mindmap-nodes-layer');
            const connectionsGroup = document.getElementById('svg-connections-group');

            if (!nodesContainer || !connectionsGroup) return;

            updateMindMapTransform();
            nodesContainer.innerHTML = '';
            
            renderConnectionsOnly();

            ws.mindmap.nodes.forEach(node => {
                const card = document.createElement('div');
                const colors = {
                    character: 'border-amber-500 bg-amber-950/40 text-amber-200',
                    event: 'border-blue-500 bg-blue-950/40 text-blue-200',
                    plot: 'border-emerald-500 bg-emerald-950/40 text-emerald-200',
                    clue: 'border-purple-500 bg-purple-950/40 text-purple-200',
                    setting: 'border-rose-500 bg-rose-950/40 text-rose-200'
                };

                const isConnectSelected = window.state.selectedConnectSourceId === node.id;

                card.className = `mindmap-node-card pointer-events-auto absolute w-48 p-3 rounded-xl border-2 backdrop-blur-md shadow-xl select-none transition-shadow ${colors[node.type] || 'border-gray-500 bg-gray-800'} ${isConnectSelected ? 'ring-4 ring-amber-400 animate-pulse' : ''}`;
                card.id = `mindmap-card-${node.id}`;
                card.style.left = `${node.x}px`;
                card.style.top = `${node.y}px`;

                card.innerHTML = `
                    <div class="node-drag-handle flex items-center justify-between pb-1.5 mb-1 border-b border-white/10">
                        <div class="flex items-center space-x-1.5 flex-1 min-w-0">
                            <i class="fa-solid fa-grip-vertical text-[10px] opacity-40"></i>
                            <span contenteditable="true" onblur="updateNodeTitle('${node.id}', this.innerText)" class="font-bold text-xs outline-none truncate block flex-1">${node.title}</span>
                        </div>
                        <button onclick="deleteNode('${node.id}')" class="text-xs text-gray-400 hover:text-red-400 p-0.5 ml-1"><i class="fa-solid fa-xmark"></i></button>
                    </div>
                    <p contenteditable="true" onblur="updateNodeDesc('${node.id}', this.innerText)" class="text-[11px] opacity-85 outline-none font-sans leading-snug min-h-[24px]">${node.desc}</p>
                `;

                card.onpointerdown = (e) => {
                    if (e.target.getAttribute('contenteditable') === 'true') return;

                    if (window.state.mindmapMode === 'pan') {
                        return;
                    }

                    if (window.state.mindmapMode === 'connect') {
                        e.stopPropagation();
                        handleNodeConnectClick(node.id);
                        return;
                    }

                    if (window.state.mindmapMode === 'node') {
                        if (e.button !== 0) return;
                        e.stopPropagation();
                        e.preventDefault();

                        if (activeNodeDrag) {
                            window.removeEventListener('pointermove', onGlobalNodePointerMove);
                            window.removeEventListener('pointerup', onGlobalNodePointerUp);
                            window.removeEventListener('pointercancel', onGlobalNodePointerUp);
                            activeNodeDrag = null;
                        }

                        activeNodeDrag = {
                            node,
                            cardElement: card,
                            startX: e.clientX,
                            startY: e.clientY,
                            initialX: node.x,
                            initialY: node.y,
                            rafId: null
                        };

                        window.addEventListener('pointermove', onGlobalNodePointerMove);
                        window.addEventListener('pointerup', onGlobalNodePointerUp);
                        window.addEventListener('pointercancel', onGlobalNodePointerUp);
                    }
                };

                card.ondragstart = (e) => e.preventDefault();
                nodesContainer.appendChild(card);
            });
        }

        function onGlobalNodePointerMove(e) {
            if (!activeNodeDrag) return;
            const drag = activeNodeDrag;
            const zoom = window.state.mindmapZoom;
            const dx = (e.clientX - drag.startX) / zoom;
            const dy = (e.clientY - drag.startY) / zoom;

            drag.node.x = Math.max(0, drag.initialX + dx);
            drag.node.y = Math.max(0, drag.initialY + dy);

            drag.cardElement.style.left = `${drag.node.x}px`;
            drag.cardElement.style.top = `${drag.node.y}px`;

            if (drag.rafId === null) {
                drag.rafId = requestAnimationFrame(() => {
                    if (activeNodeDrag) activeNodeDrag.rafId = null;
                    renderConnectionsOnly();
                });
            }
        }

        function onGlobalNodePointerUp(e) {
            if (!activeNodeDrag) return;

            window.removeEventListener('pointermove', onGlobalNodePointerMove);
            window.removeEventListener('pointerup', onGlobalNodePointerUp);
            window.removeEventListener('pointercancel', onGlobalNodePointerUp);

            if (activeNodeDrag.rafId !== null) {
                cancelAnimationFrame(activeNodeDrag.rafId);
            }

            renderConnectionsOnly();
            activeNodeDrag = null;
            performSaveOperation();
        }

        function renderConnectionsOnly() {
            const ws = currentWS();
            const connectionsGroup = document.getElementById('svg-connections-group');
            if (!connectionsGroup) return;

            connectionsGroup.innerHTML = '';

            ws.mindmap.connections.forEach(conn => {
                const fromNode = ws.mindmap.nodes.find(n => n.id === conn.from);
                const toNode = ws.mindmap.nodes.find(n => n.id === conn.to);
                if (fromNode && toNode) {
                    const x1 = fromNode.x + 90;
                    const y1 = fromNode.y + 40;
                    const x2 = toNode.x + 90;
                    const y2 = toNode.y + 40;
                    const midX = (x1 + x2) / 2;
                    const midY = (y1 + y2) / 2;

                    const g = document.createElementNS("http://www.w3.org/2000/svg", "g");
                    g.setAttribute("class", "connection-group pointer-events-auto cursor-pointer");
                    
                    g.onpointerdown = (e) => {
                        e.stopPropagation();
                    };
                    g.onclick = (e) => {
                        e.stopPropagation();
                        e.preventDefault();
                        deleteConnection(conn.from, conn.to);
                    };

                    g.innerHTML = `
                        <line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="#3b82f6" stroke-width="2.5" marker-end="url(#arrow)" />
                        <line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="transparent" stroke-width="20" />
                        <g transform="translate(${midX}, ${midY})" class="transition-transform duration-150">
                            <circle cx="0" cy="0" r="11" fill="#ef4444" stroke="#ffffff" stroke-width="2" class="shadow-md" />
                            <text x="0" y="4" text-anchor="middle" fill="#ffffff" font-size="13" font-weight="bold" font-family="sans-serif" style="pointer-events: none;">×</text>
                        </g>
                    `;

                    connectionsGroup.appendChild(g);
                }
            });
        }

        function handleNodeConnectClick(nodeId) {
            if (!window.state.selectedConnectSourceId) {
                window.state.selectedConnectSourceId = nodeId;
                renderMindMap();
                showToast("연결할 대상(두 번째) 노드를 선택하세요.", "info");
            } else if (window.state.selectedConnectSourceId !== nodeId) {
                const ws = currentWS();
                const sourceId = window.state.selectedConnectSourceId;
                
                const existingIndex = ws.mindmap.connections.findIndex(c => 
                    (c.from === sourceId && c.to === nodeId) || (c.from === nodeId && c.to === sourceId)
                );

                if (existingIndex !== -1) {
                    ws.mindmap.connections.splice(existingIndex, 1);
                    showToast("노드 연결이 해제되었습니다.", "info");
                } else {
                    ws.mindmap.connections.push({
                        from: sourceId,
                        to: nodeId
                    });
                    showToast("노드가 성공적으로 연결되었습니다.", "success");
                }

                window.state.selectedConnectSourceId = null;
                const previewLine = document.getElementById('svg-preview-line');
                if (previewLine) previewLine.classList.add('hidden');
                renderMindMap();
                performSaveOperation();
            } else {
                window.state.selectedConnectSourceId = null;
                renderMindMap();
            }
        }

        window.deleteConnection = function(fromId, toId) {
            const ws = currentWS();
            ws.mindmap.connections = ws.mindmap.connections.filter(c => !(c.from === fromId && c.to === toId));
            renderMindMap();
            performSaveOperation();
            showToast("노드 연결선이 해제되었습니다.", "info");
        };

        window.updateNodeTitle = function(id, val) {
            const node = currentWS().mindmap.nodes.find(n => n.id === id);
            if (node) node.title = val;
            performSaveOperation();
        };

        window.updateNodeDesc = function(id, val) {
            const node = currentWS().mindmap.nodes.find(n => n.id === id);
            if (node) node.desc = val;
            performSaveOperation();
        };

        window.deleteNode = function(id) {
            const ws = currentWS();
            ws.mindmap.nodes = ws.mindmap.nodes.filter(n => n.id !== id);
            ws.mindmap.connections = ws.mindmap.connections.filter(c => c.from !== id && c.to !== id);
            renderMindMap();
            performSaveOperation();
            showToast("노드가 삭제되었습니다.", "error");
        };

        window.switchMainTab = function(tab) {
            window.state.activeTab = tab;
            const btnStudio = document.getElementById('tab-studio-btn');
            const btnMindmap = document.getElementById('tab-mindmap-btn');
            const viewStudio = document.getElementById('view-studio');
            const viewMindmap = document.getElementById('view-mindmap');

            if (tab === 'studio') {
                viewStudio.classList.remove('hidden');
                viewMindmap.classList.add('hidden');
                btnStudio.className = "px-3 py-1 rounded-md transition shadow-sm bg-white dark:bg-gray-800 text-brand-600 dark:text-brand-400";
                btnMindmap.className = "px-3 py-1 rounded-md transition text-gray-600 dark:text-gray-300 hover:text-gray-900";
                renderSpreadEditor();
            } else {
                viewStudio.classList.add('hidden');
                viewMindmap.classList.remove('hidden');
                btnMindmap.className = "px-3 py-1 rounded-md transition shadow-sm bg-white dark:bg-gray-800 text-brand-600 dark:text-brand-400";
                btnStudio.className = "px-3 py-1 rounded-md transition text-gray-600 dark:text-gray-300 hover:text-gray-900";
                renderMindMap();
            }
        };

        function renderWorkspaceList() {
            const select = document.getElementById('workspace-select');
            select.innerHTML = '';
            Object.values(window.state.workspaces).forEach(ws => {
                const opt = document.createElement('option');
                opt.value = ws.id;
                opt.textContent = ws.title;
                if (ws.id === window.state.activeWorkspaceId) opt.selected = true;
                select.appendChild(opt);
            });
        }

        window.switchWorkspace = function(id) {
            window.state.activeWorkspaceId = id;
            applyStateToUI();
            renderSpreadEditor();
            renderMindMap();
            renderWorkspaceList();
            showToast(`"${currentWS().title}" 워크스페이스로 전환되었습니다.`, "info");
        };

        window.openNewWorkspaceModal = function() {
            document.getElementById('modal-workspace').classList.remove('hidden');
        };

        window.closeWorkspaceModal = function() {
            document.getElementById('modal-workspace').classList.add('hidden');
        };

        window.createNewWorkspace = function() {
            const titleInput = document.getElementById('new-ws-title');
            const title = titleInput.value.trim() || '제목 없는 소설';
            const newId = 'ws_' + Date.now();

            window.state.workspaces[newId] = {
                id: newId,
                title: title,
                activePageIndex: 0,
                activePairIndex: 0,
                book: { presetKey: 'sin-guk', widthMM: 152, heightMM: 225, isSpread: false, showMarginGuide: true, margins: { top: 20, bottom: 22, left: 18, right: 18 } },
                typography: { fontFamily: "'Gowun Batang', serif", fontSizePt: 10.5, lineHeight: 1.75, letterSpacingPx: 0, textIndentEm: 1.0, paragraphSpacingPx: 0, textColor: '#111827', paperColor: '#fdfbf7' },
                pages: [{ id: 'p1', title: '페이지 1', content: '', htmlContent: '<p><br></p>' }],
                mindmap: { nodes: [], connections: [] }
            };

            window.state.activeWorkspaceId = newId;

            titleInput.value = '';
            closeWorkspaceModal();
            renderWorkspaceList();
            applyStateToUI();
            renderSpreadEditor();
            renderMindMap();
            performSaveOperation();
            showToast("새 워크스페이스가 생성되었습니다.", "success");
        };

        window.confirmDeleteCurrentWorkspace = async function() {
            if (Object.keys(window.state.workspaces).length <= 1) {
                showToast("최소 1개의 워크스페이스가 존재해야 합니다.", "warning");
                return;
            }

            if (!confirm("현재 원고(작품)를 정말 삭제하시겠습니까? 삭제된 데이터는 복구할 수 없습니다.")) {
                return;
            }

            const targetId = window.state.activeWorkspaceId;
            
            // 1. 로컬 상태에서 삭제
            delete window.state.workspaces[targetId];
            window.state.activeWorkspaceId = Object.keys(window.state.workspaces)[0];

            // 2. 화면 UI 업데이트
            renderWorkspaceList();
            applyStateToUI();
            renderSpreadEditor();
            renderMindMap();

            // 3. LocalStorage 업데이트
            try {
                localStorage.setItem('bookcraft_studio_data', JSON.stringify({
                    activeWorkspaceId: window.state.activeWorkspaceId,
                    workspaces: window.state.workspaces
                }));
            } catch (e) {
                console.error("Local storage error:", e);
            }

            // 4. Firebase Firestore 서버에서 완전히 삭제
            if (db && currentUser && !currentUser.isAnonymous) {
                try {
                    const wsDocRef = doc(db, 'artifacts', appId, 'users', currentUser.uid, 'workspaces', targetId);
                    await deleteDoc(wsDocRef);
                } catch (err) {
                    console.error("Firestore 삭제 오류:", err);
                }
            }

            showToast("원고가 완전히 삭제되었습니다.", "error");
        };

        function applyStateToUI() {
            const ws = currentWS();
            document.getElementById('setting-preset').value = ws.book.presetKey;
            document.getElementById('setting-width').value = ws.book.widthMM;
            document.getElementById('setting-height').value = ws.book.heightMM;
            document.getElementById('setting-margin-top').value = ws.book.margins.top;
            document.getElementById('setting-margin-bottom').value = ws.book.margins.bottom;
            document.getElementById('setting-margin-left').value = ws.book.margins.left;
            document.getElementById('setting-margin-right').value = ws.book.margins.right;
            document.getElementById('setting-font-family').value = ws.typography.fontFamily;
            document.getElementById('setting-font-size').value = ws.typography.fontSizePt;
            document.getElementById('setting-line-height').value = ws.typography.lineHeight;
            document.getElementById('setting-letter-spacing').value = ws.typography.letterSpacingPx;
            document.getElementById('setting-text-indent').value = ws.typography.textIndentEm !== undefined ? ws.typography.textIndentEm : 1.0;
            document.getElementById('setting-paragraph-spacing').value = ws.typography.paragraphSpacingPx !== undefined ? ws.typography.paragraphSpacingPx : 0;
            document.getElementById('setting-text-color').value = ws.typography.textColor;
            document.getElementById('setting-paper-color').value = ws.typography.paperColor;
            document.getElementById('setting-margin-guide').checked = ws.book.showMarginGuide;

            document.getElementById('label-font-size').textContent = `${ws.typography.fontSizePt}pt`;
            document.getElementById('label-line-height').textContent = ws.typography.lineHeight === 1.0 ? '1.0 (없음)' : ws.typography.lineHeight;
            document.getElementById('label-letter-spacing').textContent = `${ws.typography.letterSpacingPx}px`;
            document.getElementById('label-text-indent').textContent = `${ws.typography.textIndentEm}em`;
            document.getElementById('label-paragraph-spacing').textContent = `${ws.typography.paragraphSpacingPx}px`;

            setSpreadMode(ws.book.isSpread);
        }

        window.openAuthModal = function() {
            document.getElementById('modal-auth').classList.remove('hidden');
        };

        window.closeAuthModal = function() {
            document.getElementById('modal-auth').classList.add('hidden');
        };

        function loadLocalStorageData() {
            try {
                const saved = localStorage.getItem('bookcraft_studio_data');
                if (saved) {
                    const parsed = JSON.parse(saved);
                    if (parsed.workspaces) {
                        window.state.workspaces = parsed.workspaces;
                        window.state.activeWorkspaceId = parsed.activeWorkspaceId || Object.keys(parsed.workspaces)[0];
                    }
                }
            } catch (e) {
                console.error(e);
            }
        }

        async function loadCloudDataForUser(uid) {
            if (!db) return;
            try {
                const wsCol = collection(db, 'artifacts', appId, 'users', uid, 'workspaces');
                unsubscribeWorkspaces = onSnapshot(wsCol, (snapshot) => {
                    if (!snapshot.empty) {
                        const loadedWorkspaces = {};
                        snapshot.forEach((docSnap) => {
                            loadedWorkspaces[docSnap.id] = docSnap.data();
                        });
                        window.state.workspaces = loadedWorkspaces;
                        if (!window.state.workspaces[window.state.activeWorkspaceId]) {
                            window.state.activeWorkspaceId = Object.keys(window.state.workspaces)[0];
                        }
                        renderWorkspaceList();
                        applyStateToUI();
                        renderSpreadEditor();
                        renderMindMap();
                    }
                }, (err) => console.error(err));
            } catch (e) {
                console.error(e);
            }
        }

        window.showToast = function(msg, type = "info") {
            const container = document.getElementById('toast-container');
            const toast = document.createElement('div');
            
            const styles = {
                success: 'bg-emerald-600 text-white',
                error: 'bg-red-600 text-white',
                warning: 'bg-amber-500 text-white',
                info: 'bg-gray-800 text-white'
            };

            toast.className = `px-4 py-2.5 rounded-xl shadow-xl text-xs font-semibold flex items-center space-x-2 transition-all duration-200 transform translate-y-2 opacity-0 pointer-events-auto ${styles[type] || styles.info}`;
            toast.innerHTML = `<i class="fa-solid fa-circle-info text-sm"></i><span>${msg}</span>`;

            container.appendChild(toast);

            setTimeout(() => {
                toast.classList.remove('translate-y-2', 'opacity-0');
            }, 10);

            setTimeout(() => {
                toast.classList.add('opacity-0', 'translate-y-2');
                setTimeout(() => toast.remove(), 200);
            }, 2500);
        };

        window.toggleDarkMode = function() {
            document.documentElement.classList.toggle('dark');
        };
    </script>
</body>
</html>
